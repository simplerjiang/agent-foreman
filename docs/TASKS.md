# Foreman 任务拆分（实现步骤）

> 把 [DESIGN.zh-CN.md](DESIGN.zh-CN.md) 的设计边界拆成**可执行、可验收**的步骤。
> 阶段对应 [ROADMAP.md](ROADMAP.md) 的 P0–P7；代码边界见 DESIGN §14：
> **shared**（两端共用）/ **client**（PC 应用 + agents）/ **server**（后端 + PWA）。
>
> 约定：每步尽量是"能独立跑、能验收"的纵向小切片；`[ ]`→`[x]` 表示完成；新发现的子任务就近补进对应阶段。

---

## P0.5 — 仓库重排（动手前的地基）
把现有扁平的 `src/foreman/{core,agents,...}` 重排成 client / server / shared 三块。

- [x] **T0.1** 建 `shared/`：迁入 `config.py`、`llm/client.py`；新增 `events.py`（AgentEvent + 事件类型 + EventBus）、`protocol.py`（wss 协议契约占位）。✅ ruff/import/4 tests 通过。
- [x] **T0.2** 建 `client/`：迁入 `agents/`、`monitor/`、`core/`（operator/auditor/gate/reviewer/scheduler/supervisor/checkpoint/events）、`store/`（本地库）；新增 `computer_use/` 占位。✅ 三关通过（review/security/PM）；client⊥server 边界双向验证。
- [x] **T0.3** 建 `server/`：迁入 `server/{app,push,auth}.py`、`web/`（PWA 前端归这里）；新增 server `store/`（服务器库）占位。✅ 三关通过；/health+web 200，server⊥client，双库无表名冲突。
- [x] **T0.4** 改 `__main__.py`：`foreman app`（client）/ `foreman serve`（server）/ `foreman dispatch`；`pyproject.toml` 入口 + 可选依赖分组（client / server extras）。✅ 三关通过；deploy 脚本改 `.[server]`。
- [x] **验收**：`foreman serve` 仍能起 `/health`；`foreman.client` 与 `foreman.server` 各自独立可导入。✅ 本地 /health 200 + 12 tests；线上部署验证见 T0.4 提交。

## P1 — 单机驱动（client 为主）
dispatch 一个任务 → 看 claude/codex 在真实工作区跑 → 窗口/浏览器看实时事件。

**shared**
- [x] **T1.1** 定稿 `AgentEvent` 与事件类型枚举（DESIGN §7.1）；时间戳 UTC ISO8601。✅ `make_event()`（校验 type）+ `utc_now_iso()`；三关通过；16 tests。
- [x] **T1.2** LLM client 跑通最小调用（OpenAI 兼容 + Anthropic 兼容），读 base_url/model/key。✅ `transport=` 注入 + MockTransport 测两种线格式；三关通过；19 tests。

**client / agents**
- [x] **T1.3** `ClaudeCodeAdapter.start`：spawn `claude -p "<instr>" --output-format stream-json --verbose`（Windows：`claude.cmd`、UTF-8、creationflags）。✅ start+stop+`_build_cmd`+`_spawn` 可测seam；argv 列表(无注入)；三关通过；23 tests。
- [x] **T1.4** `ClaudeCodeAdapter.stream`：逐行 `json.loads` → 映射 `AgentEvent`。✅ result→stop 其余→agent_output；非 JSON/非对象→raw；三关通过；24 tests。
- [x] **T1.5** `ClaudeCodeAdapter.stop`（+ `send`/`--resume` 占位给 P4）。✅ stop（T1.3）+ stream 捕获 `native_session_id`；send/interrupt 明确 P4/P3 stub；三关通过；25 tests。
- [x] **T1.6** `CodexAdapter.start/stream/stop`：`codex exec`，输出解析。✅ 抽出 `SubprocessCliAdapter` 基类，claude/codex 皆瘦子类；共享 `_fakes`；三关通过；29 tests。
- [x] **T1.7** `Runner.launch`：选 adapter → 起 agent → 每个事件 **落库 + 上 EventBus**；两个 CLI 可并行不同会话。✅ 后台 _pump（先落库后上总线）+ wait()；未启用 agent 报错；三关通过；35 tests。

**client / store**
- [x] **T1.8** 本地 SQLite：`sessions / tasks / events` 表 + 读写；`schema_version`。✅ add/get sessions·tasks·events（payload→JSON）；init 记 schema_version；三关通过；33 tests。

**client / CLI + 本地 UI**
- [x] **T1.9** `foreman dispatch "<task>" --workspace <path> --agent claude-code|codex`：建 Root Session → `Runner.launch`。✅ `client/dispatch.py`（build_session_task + run_dispatch，可注入）；CLI 懒加载保边界；三关通过；38 tests。
- [x] **T1.10** 本地 API：`GET /api/sessions`、`GET /api/sessions/{id}/events`、`WS /ws`（EventBus→前端）。✅ store=None→503；WS 先回放后实时、按会话过滤、断连处理；EventBus 加 subscribe_queue；三关通过；42 tests；线上 /health 200 + /api 503（无数据泄露）。
- [x] **T1.11** 最小时间线页（`server/web/`）：列会话 + 实时事件流。✅ app.js 接 /api/sessions + WS；textContent 防 XSS；**Playwright/chromium 浏览器验收通过**（3 事件渲染+截图）；三关通过；44 tests。
- [x] **T1.12** `foreman app`：pywebview 原生窗口套本地 UI + pystray 托盘（开=上线/关=下线）。✅ `start_local_app`（后台 uvicorn 线程，可测）+ app_cmd 懒加载 pywebview 窗口（无则无头回退）；关窗=下线；托盘**暂缓**（避免未测 GUI 循环冲突）；三关通过；45 tests。窗口本身需桌面运行验证（UI 已在 T1.11 浏览器验收）。
- [x] **验收**：`foreman dispatch ... --workspace D:\proj`，窗口/浏览器实时看到 claude 与 codex 的事件流入。✅ dispatch 落库（T1.9）→ /api + /ws 取（T1.10）→ 时间线页渲染（T1.11 浏览器验收）→ `foreman app` 窗口承载（T1.12）。45 tests 全绿。

## P1.5 — 多语言（i18n）+ 输出语言（DESIGN §15）
中/英切换：不只换界面，还贯穿提示词与 LLM 返回语言。
- [x] **T1.13** 语言设置 + 输出语言指令：config `ui.language`（默认 zh）+ 运行时 `config_kv` 覆盖 + `GET/POST /api/settings/language`；shared `language_directive(lang)`；预留接入 Operator/Auditor/Reviewer/Briefing 的 system prompt。✅ shared/i18n + ConfigKV + 端点；三关通过；48 tests。
- [x] **T1.14** UI 语言切换：header zh/en 切换 + 文案字典 + localStorage + 同步后端；**浏览器验收**（切换后界面文案随之改变，截图）。✅ data-i18n + I18N 字典 + 切换标签翻转 + 会话区重渲染；**Playwright 验收通过**（时间线 时间线→Timeline）；三关通过；49 tests。

## P2 — 观测 + 审阅 + 检查点 + 看门狗（client）
- [x] **T2.1** 工作区非仓库时 `git init`。✅ `ensure_repo()`（rev-parse 检测 + git init，argv 无 shell；子目录仓库不重复 init）；三关通过；52 tests。
- [x] **T2.2** Checkpoint Manager：临时索引 `add -A`→`commit-tree`→影子 ref `refs/foreman/ckpt/*`（§6.5）；记 `checkpoints`。✅ `snapshot()`（`GIT_INDEX_FILE` 临时索引→`add -A`→`write-tree`→`commit-tree` 串父链→`update-ref` 影子 ref；`core.autocrlf=false`；不碰暂存区/分支/历史；遵守 .gitignore 不收机密）+ `Store.add/get_checkpoints`；argv 无 shell；三关通过；59 tests。
- [x] **T2.3** 一键回退：恢复到某检查点（含删后建文件）+ 先给当前打点（redo）。✅ `undo_to()`（先 `snapshot` 当前为 redo 点→`_restore_worktree`：`rev-parse` 校验目标 commit→临时索引 `read-tree`+`checkout-index -af` 重写目标文件→删检查点后新建文件→prune 空目录；`core.autocrlf=false` 逐字节一致；遵守 .gitignore 不删机密/node_modules）+ `resolve_step()`；argv 无 shell；agent 状态复位延后至 P4 决策回路层（持有 Runner）。三关通过；66 tests。
- [x] **T2.4** Hook 接收端 `POST /hooks`（PreToolUse/PostToolUse/Stop/Notification）。✅ `HookReceiver`（map→落库→上总线，persist-first 同 Runner；PreToolUse 走 Gate.classify，命中 requires-approval → 记 `approval_req` + 回 deny 决策体，curl 把它回传给 Claude Code 即拦下危险工具）+ `hook_to_event`/`action_text` 纯函数；route `POST /hooks` 在 server/app.py 但**仅依赖 shared**、receiver 注入（同 store/bus，team `serve` 不注入→503，hook 是本地端点 §4.3）；hook 名取 X-Hook 头→body `hook_event_name`；session 关联取 `?session_id`→Claude 原生 id；新增事件类型 `notification`（§4.1/§5.6 看门狗「在等输入」信号，§7.1 枚举漏列，补上）。**完整审批回路（推手机+等+resume）延后 P3**（Gate.request_approval）。三关：①Code Review——diff 小且加性，payload/非 dict/空 body 皆兜底，无 shell 执行（action_text 仅做子串分级）；②安全——/hooks 本地 127.0.0.1、无命令注入、不碰 secrets、deny 回显仅本地、team 端 503 不暴露，鉴权属 T3.5；③PM 验收——对照验收：四类 hook + SubagentStop 全覆盖、PreToolUse↔Gate 联动拦危险动作（§4.3/§6.6）达成。82 tests 全绿 + ruff 通过。
- [x] **T2.5** Git watcher（diff/commit）+ process/idle（psutil）→ 刷新 `last_progress_at`。✅ 三个廉价观测源喂看门狗的 `last_progress_at`（§4.1/§4.3）：①`ProgressTracker`（内存版 last_progress_at 注册表，注入时钟；`touch/last/idle_seconds/is_idle/drop`，stdout/hook/git/CPU 任一信号都 `touch`）；②`GitWatcher.poll`（`rev-parse HEAD`+`status --porcelain` 比对上次状态→`git_commit`/`git_diff` 事件，payload **仅计数不含文件名/内容**；任一 git 变化 `touch` tracker；persist-first 再上总线；首轮立基线；`runner` 注入 seam 无 shell；新增 `drop()`）；③`ProcessWatcher.poll`（psutil 经注入 `sampler` seam：进程存活 + 累计 CPU 增量判活；CPU 推进→`touch`；按 **同一 pid** 比对 delta，重启/PID 复用同 key 自动重立基线不误报；死进程丢基线；读不到→unknown 不扰基线）。`watch()` 皆为定时轮询包壳。三关：①Code Review（subagent）——基线/死进程/PID 复用/浮点阈值边界齐备，importorskip 守 psutil 缺失，风格对齐 test_hooks；②安全（subagent）——无 shell/命令注入、git_diff 仅计数不泄漏路径内容、psutil 仅读传入 pid、AccessDenied 安全降级 unknown、补 GitWatcher.drop 防长跑泄漏；③PM 验收——对照 §4.1/§4.3：三观测源齐、皆刷新 last_progress_at、idle 判定就绪供 T2.6。108 tests 全绿 + ruff 通过。
- [x] **T2.6** Supervisor 看门狗（**全局唯一**）：池健康状态机 + 廉价巡检 + 可疑时升级 LLM + 恢复 playbook（§4.1/§5.6）。✅ `Supervisor`（client/core）= 单个全局看门狗：`pool: dict[key→AgentRecord]`，`register/set_pid/mark_done/unregister` 管池成员；`AgentRecord.state ∈ {starting,running,idle,waiting_input,stalled,errored,dead,done}`（§4.1 八态全覆盖）+ 经 `ProgressTracker`（T2.5）读 `last_progress_at`。**两层巡检**：①`classify()` 纯函数廉价确定性（不花 token）——优先级 进程死>tail报错>tail等输入>stalled>idle>running；阈值**按 agent 类型分设**（codex idle60/stall150 比 claude 120/300 更紧，§4.1）；进程存活/stdout 尾巴皆为注入 seam（`liveness`/`tail_provider`，无 seam 则纯 idle 判定），时钟/`now` 注入可测。②`judge` 仅在「可疑」且已接 LLM 时调用、**绝不每轮**（测试验证 not-suspicious→judge 零调用）。`poll_once` 扫全池：快照迭代 + **逐 agent try/except**（单点抛错只记一条 `error` 事件、不拖垮整圈，§4.1「单点要稳」）。**恢复 playbook**`plan_recovery`：DEAD→restart_from_checkpoint（连崩>max_restarts→escalate_card）/STALLED→nudge/WAITING_INPUT→answer_or_card/ERRORED→backoff_or_card（对齐 §4.1 处置表）；状态变化才发 `health`/`stall`/`recover` 事件（持续同态保持安静），persist-first 同 Runner。`LLMJudge`（②）拼提示词时**追加 `language_directive`**（§15），tail 先脱敏后限长再外发。**恢复执行（真 nudge/interrupt+resume/检查点重启）延后 P4**（决策回路持有 Runner 两向控制；Runner.send=P4 stub、interrupt=P3 stub、§6 Gate 在 P4）——本任务交付「发现+分类+规划+升级事件」，`recover` 事件含 `execution_deferred=True`。**失败转移建议（换 CLI）**作为 P4 卡片内容延后。三关：①Code Review（subagent）——precedence/阈值数学/连崩 escalate 计数/变更才发事件/逐 agent 隔离 皆正确；按建议修：judge 覆盖只许 `_JUDGE_ALLOWED`（防 rogue judge 伪造 DEAD/DONE/STARTING）、tail 每 tick 只读一次、fail_count 任何非 DEAD 即清零；②安全（subagent）——无 subprocess/shell/eval、事件载荷仅元数据不含 stdout/路径、key 由 LLMClient 管不外泄；按建议修：`error` 载荷用 `type:msg` 截 200（防未来 seam 泄路径）、`redact_secrets` 在 tail 外发前掩码 sk-/Bearer/api_key/gh_ 凭据形；③PM 验收——对照 §4.1/§5.6：全局唯一+八态机+廉价巡检+可疑才升级 LLM+恢复 playbook 全达成（执行层 P4，已标注）。**132 tests 全绿（+24 本任务）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared）。
- [x] **T2.7** Reviewer：检查点处 diff+目标 → 结构化评审。✅ 与 Auditor 互补（Auditor 管动手前、Reviewer 管动手后，§4.1）：`Reviewer.review(goal, diff, *, context, qa_standard)` 把「检查点处 diff + 目标 + QA标准 + 相关文件片段」喂 LLM → 结构化 `ReviewResult{verdict, summary, risks[], suggestions[], needs_human}`，`verdict ∈ {approve, request_changes, escalate}`（§186-187）。**diff 来源**：新增 `CheckpointManager.diff(from_ref, to_ref=None)`——临时索引快照活动工作区与检查点对比，**新建未跟踪文件也进 diff**（裸 `git diff <commit>` 会漏）；`rev-parse --verify` 拒 option-like 输入；`add -A` 遵守 .gitignore 故 .env/secrets/node_modules 永不进 diff。**§15**：system prompt 追加 `language_directive`（stub 漏了，本次补上）。**从严默认（§6.7）**：`parse_review` 解析失败/未知 verdict/空回复 一律 `escalate`+`needs_human=True`，绝不静默 approve；escalate 强制 needs_human；容错抽 JSON（```json 围栏 / 散文中嵌对象 / 标量→list 强转）；diff 超长截断控 token。三关：①Code Review——verdict 闭集校验+大小写归一、围栏/散文抽取、list 强转去空、diff 含新文件且 tree-vs-tree 正确；②安全——全 argv 无 shell、rev-parse --verify 防注入、diff 遵守 .gitignore 不泄机密、仅发用户自有 LLM 端点（同 T2.6 信任边界）；③PM 验收——对照 §4.1/§5.3/§186：检查点 diff+目标+QA+片段输入、四字段结构化输出、三态 verdict、§15 语言指令 全达成。**执行层延后**：request_changes→Runner.send（P4 stub）、escalate→Gate→卡→一键回退（Gate P3/P4；T2.3 `undo_to` 已备回退机制）；live LLM key mock 测+延后（同 T2.6）。**154 tests 全绿（+22）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared）。
- [x] **验收**：任务完成自动产出评审；任一步可一键回退；卡死/崩溃能被发现并恢复或升级。✅ 新增**阶段级集成测试** `tests/test_p2_acceptance.py`（2 例）把 P2 各块串成一条端到端流，跑在**真实 git 工作区 + 真实 SQLite Store + 真实 EventBus + 真实 ProgressTracker** 上（无网络/无 token，Reviewer 走 httpx.MockTransport）：①任务完成→自动评审——`CheckpointManager.diff(预检查点)`→`Reviewer.review`→结构化 verdict（escalate+needs_human），persist-first 落 `review` 事件再上总线；②一键回退——`undo_to(预检查点)` 把工作区**逐字节复原**（改动还原/新文件删除/被删文件重建）+ 留 redo 点，验证 Reviewer escalate→「⛔撤掉重来」路径真能跑；③卡死/崩溃发现并恢复或升级——单一全局 Supervisor 廉价巡检同时识别 STALLED→`nudge`、DEAD→`restart_from_checkpoint`，连崩>max_restarts→`escalate_card`（升级弹卡），且 LLM judge **仅对可疑 agent 调用、绝不调健康 agent / 不每轮调**；事件 persist-first 落库+上总线双验。三关：①Code Review——时钟注入确定性（codex idle200≥stall150、claude idle10=running、liveness 优先判 DEAD）、连崩计数 1→4 跨 max_restarts、payload 经 json.loads 读；②安全——纯测试无 shell/无注入/无真机密/零网络出网；③PM 验收——对照 ROADMAP P2「Done when」三条验收逐条有据达成（非仅单测绿）。**156 tests 全绿（+2）+ ruff 通过**。**P2 阶段完成**。

## P3 — 手机面 + 审批 + server 起步
- [x] **T3.1** server 库：accounts / access_keys / process_registry（占位 invites / cache）。✅ 在 `ServerStore`（server/store/db.py）上补齐三张表的读写助手，模型/引擎此前已搭好（T0.3）：①**accounts**——`add_account`（admin 建号、created_at 自动盖戳）/`get_account`/`get_account_by_username`/`get_accounts`/`set_account_status`（停用/启用，缺号 no-op）；②**access_keys**——`add_access_key`（**仅存哈希**，明文只在创建时给一次）/`get_access_keys`（active+revoked，新→旧）/`get_access_key_by_hash`（握手路径用，含 revoked 让调用方判 status/expiry）/`revoke_access_key`（**单张吊销、其余照常**，对齐 §7.2「一机一张、可多张、哈希存、可吊销」）/`touch_access_key`（last_seen_at）；③**process_registry**——`register_process`（**按 id upsert**，首插盖 created_at、复注更新原行并返回持久化行）/`set_process_online`（在线/离线+心跳，App 开=上线关=下线 §4.6）/`get_processes`（一人可多机）/`get_online_processes`（可选按账号 scope，供总机路由 §8.5）；`init()` 补记 server `schema_version`（§11.1，与 client 各表名独立无冲突）；`session(expire_on_commit=False)` 使返回行可读。**invites/cache 仅建表占位**（admin 邀请 T7.2 / 展示缓存 T7.5，本任务不加助手）。三关：①Code Review——upsert 更新路径改为返回持久化行（避免回传 created_at 为空的入参对象）、`col(online).is_(True)` 布尔过滤正确、列表稳定排序；②安全——纯 SQLModel 参数化查询无注入、无 shell/subprocess/eval、**access key 只存哈希永不存明文**、server 库不含 LLM key/秘方/diff（§7.2/§8.3）、多租户查询按 account_id 隔离（timing-safe key 比对属 T3.5 鉴权层，已注）；③PM 验收——对照 §7.2：三张主表读写齐全 + 哈希存/单张吊销 + 在线注册与路由查询 + invites/cache 占位达成。**165 passed +2 skipped（+11 本任务）+ ruff 通过**。server⊥client 边界守住（纯 server/store + shared）。
- [x] **T3.2** `wss /relay`：出站长连 + access key 握手 + 心跳 + 重连 + 按账号路由（§8.5）。✅ 双端实现，共用 `shared/protocol.Envelope`（补齐 `to/from_dict|json` + `KIND_*` 帧词汇 + 容错解析：坏帧→空信封不抛）。**server**（`server/relay.py` 纯 server/store + shared，无 client 泄漏）：`Relay`①握手——`authenticate(payload)` 取 hello 帧 `access_key`→**仅按哈希查**（`auth.hash_access_key` sha256；新增 `generate/hash/verify_access_key`，`verify` 走 `compare_digest`）→拒 缺失/未知/revoked/过期(ISO8601 字典序比)/账号停用；**account_id 一律由 key 反查、绝不信客户端帧**（防越权）。②路由——`conns: account_id→[RelayClient]`，`route(account_id, env, process_id=)` **仅向该账号**在线连接转发（0=无人在线→调用方走展示缓存）。③心跳——`_on_frame` 收 heartbeat→刷 `last_heartbeat`+回 pong。④生命周期——`serve(ws)` 瘦壳（鸭子类型 accept/receive_json/send_json/close）：握手→`register`(标 online+`touch_access_key`)→try/finally 收尾 `unregister`(标 offline)，**崩溃也不会留 online 僵尸**；失败握手回 `hello_ack{ok:False}`+close(1008)、**不注册任何东西**；连上/掉线各发一条 `health` 事件上总线。`app.py` 注入 `relay=`（个人模式无 relay→accept+close 1008，不 404）。**client**（`client/relay.py` 纯 shared，无 server 泄漏）：`RelayConnector`——`hello()` 首帧带 key+机器名；`run_once` 握手(非 hello_ack→ConnectionError 重连；显式 ok=False→`RelayAuthError` 致命不重试)→读帧循环回 pong；`run()` **指数退避重连**（`backoff_delay` base*2^n 封顶），**仅在「握手成功且会话存活≥一个退避周期」才重置退避**——避免「认证后秒断」relay 被 1s 死循环锤；`RelayAuthError` 直接停（坏 key 重试无意义）。传输鸭子类型（注入 `connect`/`sleep`/`clock`），默认 `connect` 懒加载 `websockets`（client extra 新增，缺包不影响导入）。三关：①**Code Review**（subagent）——退避状态机/握手解析/register-unregister 配对/过期比较/容错 全核对；按建议修：握手要求 `ok is True`（非 ack→重连而非误判成功）、连上健康+ack 移入 try（publish 抛错不漏注册）、退避加最短会话守卫防秒断锤。②**安全**（subagent）——**修复 HIGH：client 控的 `process_id` 是注册表主键→可越账号覆盖他人机器行**：改为 `process_id = auth.key_id`（**服务端由 key 派生、账号绑定、不信客户端**），并在 `register_process` 加**所有权守卫**（既有行属他账号则拒绝改写、原样返回）双保险；确认 仅存哈希/account_id 服务端派生/route 不跨账号/无明文回显/无注入(仅 json)/掉线必清理。③**PM 验收**——对照 §8.5：①握手认身份 ②按账号路由 ③心跳+断线重连 ④安全(TLS 内传 key/仅哈希) 逐条达成。**live 接入延后**（真 wss 拨号 + Relay/ServerStore 注入 serve + 客户端发起的周期心跳定时器 = 团队上线，归 T7.1/live rollout，已标注；本任务交付契约+握手+pong+退避+路由，全 mock/单测）。**190 tests 全绿（+15）+ ruff 通过**。client⊥server 边界双向无泄漏。
- [x] **T3.3** PWA：manifest + service worker + Web Push（VAPID）。✅ 三块齐活（§4.6）：①**manifest**——补 `id/scope/standalone/orientation/lang/categories`，可安装；②**service worker**（`sw.js`）——`push` 事件→`showNotification`（title/body 纯文本非 HTML，无 XSS；带 tag/icon/badge，`data.actions` 预留 T3.4 一键批/驳），`notificationclick`→聚焦+**同源校验后**深链（`new URL(raw, origin)` 仅放行同源 path，挡 `javascript:`/跨站）+ `postMessage(action,url)` 给页面（T3.4 用）；③**Web Push（VAPID）**——`Pusher.send`（`server/push.py`，纯 shared）真正实现：`pywebpush`（注入 seam 可测）跑在 `asyncio.to_thread`（不堵事件循环）+ `vapid_claims={sub}`，**按状态分级**返回 SENT/DISABLED/GONE/ERROR（404/410=GONE→调用方剪订阅，其余=ERROR 留着重试），`send_to_all` 收集 GONE 端点；`subscription_info` 兼容「行/浏览器嵌套 keys/扁平」三形。**订阅存本地库**（§7.1 `push_subscriptions`，client `Store`）：`add_push_subscription`（**按 endpoint upsert，不产重复行**；endpoint 加 `unique+index`）/`get`/`delete`，取原语不取模型故 server `app.py` 仍**只依赖 shared**（§14 边界）。**端点**（`app.py`，store 鸭子类型注入）：`GET /api/push/vapid-public-key`（**只返公钥**，私钥永在 `.env`/`cfg.secrets` 不经任何端点；无钥→`enabled:false` 前端自动跳过）、`POST /api/push/subscribe`（存订阅；server-cache 库无此助手→503）、`POST /api/push/unsubscribe`。**前端**（`app.js`）：`enablePush` 全流程——取公钥→`urlBase64ToUint8Array`→请求通知权限→`pushManager.subscribe`→POST `toJSON()`，多重特性检测+优雅降级。**附带修** `scripts/gen_vapid.py`：原 `public/private_key_urlsafe_base64()` 在装好的 py-vapid 上不存在（潜伏 bug）→改用 cryptography 取「公钥=未压缩 EC 点(65B)、私钥=32B 标量」base64url 无填充，正是浏览器/pywebpush 要的格式。三关：①**Code Review**（subagent）——边界无 client 泄漏、404/410 分级、upsert 无重复、payload/claims 形状 皆核对，APPROVE-WITH-NITS→按建议修：endpoint 加 `unique+index`、`keys` 空字典不静默落扁平分支；②**安全**（subagent，PASS-WITH-NOTES）——确认 私钥零泄漏、SQLModel 参数化无注入、`showNotification` 纯文本无 XSS、p256dh/auth 只落本地库不进服务器缓存；SSRF（pywebpush 发往订阅 endpoint）/endpoint 无长度上限 在「单用户本地库」信任模型下属**接受风险并延后**（团队 push 上线再加 scheme 白名单），已加同源深链守卫；③**PM 验收**——对照 §4.1/§4.6：manifest 可安装 + SW 收推 + VAPID Web Push 管道（订阅/取消/公钥/发送+剪枝）齐备达成。**浏览器验收**（Playwright/chromium 持久上下文 headless）：页面渲染「🦺 Foreman + 开启通知」+ SW `ready` 激活 + manifest standalone/scope + `/api/push/vapid-public-key` 返真 87 字符公钥 + 点「开启通知」跑通订阅流（到 push service 边界优雅停）+ **浏览器→`/api/push/subscribe`→本地库 1 条订阅** 全链路，截图留证。**202 tests 全绿（+12）+ ruff 通过**。client⊥server 边界守住（push.py/app.py 纯 shared + 本地库存订阅）。**Live 投递延后**（凭据/基建门槛，已标注）：真 `PushManager.subscribe` 需浏览器对接 FCM/Mozilla push service + HTTPS 源；真发送需 `.env` 里真 VAPID 私钥（`scripts/gen_vapid.py` 生成）。本任务交付契约+管道+订阅存储+前端流，全 mock/单测+浏览器验收到 push-service 边界；真接入归 T7/live rollout。一键批/驳的 SW actions 点击落到审批端点归 **T3.4 Gate**。
- [x] **T3.4** Gate：危险动作分级 + 审批卡推送 + 批/驳闭环（§6.6）。✅ 把 T2.4/T2.6/T3.3 延后的审批回路真正落地（§6.6/§6.8）：①**分级**——`Gate.classify` 仍是确定性三档（safe/needs-strategy/requires-approval），硬危险**按规则拦、不靠 LLM**（§6.7① 绝不让 LLM 当唯一闸门）；②**持有+推卡**——`Gate.request_approval`：建一条 pending `approvals` 行（带**一次性 nonce** §6.8 防重放）→ 经**注入的 Pusher**（client Gate **不 import server**，local_app 注入，§14 边界）把审批卡推到所有订阅（卡含一句话动作 + ✅批准/⛔驳回 一键 action + 同源深链 `/?approval=<id>`），GONE(404/410) 端点顺手剪枝；无 store 优雅 no-op（裸 Gate 仅 classify）。③**批/驳闭环**——`GET /api/approvals`（待办队列，返 JSON dict 不返模型，app.py 仍纯 shared）+ `POST /api/approvals/{id}`（`Gate.resolve`：**校验 nonce（`secrets.compare_digest` 时序安全）+ 一次性**——`decide_approval` 在自有 session 内复查 `status==pending` 原子收口，二次决策/重放→409；空 nonce 行永不可决；approve/reject→改状态 + persist-first 落 `approval_decided` 事件上总线）。HTTP 映射 400/403/404/409/503 齐备。**HookReceiver** 改为：危险 PreToolUse → `gate.request_approval` 建真审批+推卡 → 仍回 deny 拦下 Claude Code 工具 → `approval_req` 事件带 `approval_id` 关联；裸 Gate 走回退仍记事件（旧测试不破）。**PWA**：`#approval-list` 渲染待审卡（**全 textContent 防 XSS**，动作文本是不可信 agent 输入）+ 批/驳按钮带 nonce POST + SW message/**冷启动深链 `?approval=&action=` 一键决策**（无窗口的手机场景也能一点到位，用完 `history.replaceState` 抹掉一次性参数）。**三关**：①Code Review（subagent）——nonce/重放/TOCTOU、一次性、HTTP 映射、边界 全核对，APPROVE；按建议**加固空 nonce 行不可决** + **补冷启动一键**（原只 focus-postMessage、冷开仅导航不决策）；②安全（subagent）——确认 时序安全 nonce、不可重放/伪造、**Gate 绝不执行动作文本**（执行延后 P4，`execution_deferred=True`）、SQLModel 参数化无注入、textContent 无 XSS、卡/事件/行**不带 secrets/key/全文**、Push 体 VAPID 端到端加密、同源深链守卫、client⊥server 边界；端点无鉴权属 T3.5（同 /hooks·/api/* 既有模式，**接受并延后**，已注明 nonce 仅作重放令牌、勿当鉴权因子）；③PM 验收——对照 §6.6/§6.8：三档分级 + nonce 审批卡推送 + 批/驳闭环 全达成；**Playwright/chromium 浏览器验收通过**（seed `git push` 被拦的 pending 审批 → 页面渲染审批卡 → 点✅批准 → POST 带 nonce → DB pending→approved + `approval_decided` 事件 + 队列清空；另测冷启动深链一键 approve；双截图留证）。**219 tests 全绿（+17）+ ruff 通过**。**恢复（resume 被拦 agent）延后 P4**——hook 已 deny 拦下工具，真正"放行恢复"需 Runner 两向控制（Runner.send=P4 stub / interrupt=P3 stub），`approval_decided` 已标 `execution_deferred=True`（同 T2.6/T2.7 执行层延后惯例）。client⊥server 边界双向无泄漏（Gate 纯 client/core+shared、注入 Pusher；app.py 纯 shared）。
- [x] **T3.5** 鉴权：用户登录 + access key 管理。✅ 团队模式服务端鉴权落地（§8.2，纯 server/store + server.auth + shared，app.py 仍只依赖 shared、AuthManager 注入，§14 边界）：①**密码哈希**（`server/auth.py`）——`hash_password`/`verify_password` 用 stdlib **PBKDF2-HMAC-SHA256**（240k 迭代 + 每密码 16B 随机盐），自描述格式 `pbkdf2_sha256$iters$salt$hash`（日后可提工作因子不废旧哈希），`compare_digest` 恒时比、坏哈希/空输入兜底 False；高熵 access key / 登录 token 仍走 sha256（§8.4 仅存哈希）。②**用户登录**（§8.2，区分「人登录 PWA」vs「本地进程 access key」）——`AuthManager.login`：核验用户名+密码→发 bearer token（建 `auth_sessions` 行，**仅存 token 哈希**，明文只给浏览器一次）；**统一返回 `invalid`** 不泄漏「用户不存在/密码错/账号停用」，且**缺/停用用户路径照样跑一次 PBKDF2**（对一枚一次性 throwaway 哈希）防响应**时序枚举用户名**；`resolve_token`→（哈希查 session→**datetime 比过期**而非脆弱的字典序、过期顺手**剪枝**防表无限增长→**每次复查 account.status==active**，停用账号即时失效即便 token 未过期）；`logout` 删 session 行。③**access key 管理**——`create_access_key`（生成明文**仅此一次**返回、只存哈希 §8.4）/`list_access_keys`（**仅元数据**白名单字段，绝不出哈希/明文）/`revoke_access_key`（**带所有权校验**：吊销非自己的 key 返 `not_found`/404 不泄漏存在性 §8.4，单张吊销不影响其余 §7.2）；④**建账号**`create_account`（admin 操作、**无自助注册** §8.2，用户名唯一、role 非 admin 即 member；admin 控制台 UI 归 T7.2）。**REST 端点**（app.py，`auth` 注入；`_bearer_token` 严格解析 `Authorization: Bearer`，`require_account` 守所有受保护端点）：`POST /api/auth/login`（401 坏凭据/503 无 auth 管理器）、`POST /api/auth/logout`（幂等、无 auth 也回 ok）、`GET /api/auth/me`、`GET/POST /api/keys`、`DELETE /api/keys/{id}`（404 非自有）；**个人模式不注入 auth → 这些端点 503**（PC 自托管 UI、无账号体系；单用户远程靠隧道+既有 bearer token，不回归）。**库**：Account 加 `password_hash`、新增 `auth_sessions` 表（仅哈希）+ `set_account_password`/`get_access_key`/`add|get|delete_auth_session` 助手；server schema_version 升 **v2**（迁移器 P5）。三关：①**Code Review**（subagent）——PBKDF2 正确性/token 过期比/所有权校验/401·404·503 映射 全核对，APPROVE；按建议修：**过期改 datetime 比**（除字典序微秒位脚雷）+ 过期剪枝、**login 时序均一**（throwaway 哈希）防枚举、清理误导/garbled docstring；②**安全**（subagent，SOUND，无 CRITICAL/HIGH）——确认 仅存哈希、恒时比、`require_account` 无旁路、多租户隔离（跨账号吊销 404 不泄漏）、登录不可枚举、停用即时锁出、SQLModel 参数化无注入、无 shell/eval、无机密入 git；低危项（登录限流/会话定期清理）记为后续；③**PM 验收**——对照 §8.2/§8.4：管理员建号 + 用户登录发 token + access key 增删查（多张/哈希存/单张吊销/所有权）+ 多租户隔离 全达成，且**端到端验证**：AuthManager 铸的 key 经 **Relay（T3.2）握手认证通过、吊销后被拒**——鉴权层与中继契约一致。**235 tests 全绿（+16）+ ruff 通过**。server⊥client 边界守住。**Live 团队上线延后**（同 T3.2 惯例）：把 ServerStore+AuthManager+Relay 注入实跑的 `foreman serve`、PWA 登录页/key 管理 UI 归 T7.1/T7.2 团队上线；本任务交付鉴权内核+端点+全单测/端到端。新发现子任务（已记）：`auth_sessions` 定期清理过期/孤儿行（剪枝已覆盖被复查的 token，定时清理归 P7/运维）；`/api/auth/login` 限流/锁定防在线爆破（归 P7 团队上线加固）。
- [x] **验收**：claude 想 `git push` 被拦 → 手机收推送 → 点批准 → 恢复。✅ 新增**阶段级集成测试** `tests/test_p3_acceptance.py`（3 例）把 P3 各块串成端到端流，跑在**真实 client Store + 真实 server ServerStore（SQLite）+ 真实 EventBus + 真实 FastAPI app（TestClient，手机真正打的那张面）**上（无网络/无 token/无 live VAPID：Web Push 走 fake Pusher、relay 握手逻辑直跑）：①**被拦→手机收推送→点批准**——危险 `git push` 的 PreToolUse hook 被 `deny` 回 Claude Code（工具拦下）→ Gate 落一条 pending `approvals` + 经订阅推一张审批卡（带一句话动作 + ✅批准/⛔驳回 一键 action + 同源深链 `/?approval=<id>`）→ 手机经**真实 `POST /api/approvals/{id}`**（PWA 实际走的路由，nonce 从队列读，仿 PWA）点批准 → status→approved + `approval_decided` 事件（persist-first 落库+上总线）+ 待办队列清空；②**驳回 + 防重放**——同一回路的驳回分支 + §6.8 一次性/nonce 重放守卫经真实 HTTP 面验证（坏 nonce→403 留 pending、驳回→200、重放已决→409 首决不动）；③**团队远程安全底座**——AuthManager 铸的 access key 经 **Relay 握手认证通过**（account_id 服务端由 key 派生、伪造帧被忽略）+ 用户登录发 bearer + **吊销后握手即被拒**（T3.5 ⨯ T3.2，P3 远程手机访问的安全根基）。三关：①Code Review——纯新增测试文件、真实基础设施、断言精确无 clock/网络故无 flake、`create_app_for` 运行时调用先于文义定义合法；②安全——纯测试无 shell/无机密/无网络出网，且**正向验证**安全属性（nonce 重放/伪造 account_id 忽略/吊销切断握手）；③PM 验收——对照 ROADMAP P3「Done when」逐条达成（被拦+手机响+点批准），「恢复（resume 被拦 agent）」执行层 deferred 至 P4 决策回路（`execution_deferred=True` 已断言；需 Runner 两向控制 send/interrupt，已在 T2.6/T3.4 标注）。**238 tests 全绿（+3）+ ruff 通过**。**P3 阶段完成**。client⊥server 边界守住（纯测试横跨两库但生产码未动）。

## P4 — 决策回路 + 两向控制 + 能力层（⭐ 核心交互）
- [x] **T4.1** Operator：精简输出 + 提案下一步动作。✅ `Operator`（client/core）= 决策回路的「驾驶员」（§6.1/§4.1）：`observe(goal, agent_output, *, context, recent_actions)` 把 agent 最新原始输出喂 LLM → 结构化 `OperatorResult{summary, state, proposals[]}`：①**精简**——`summary` 一句话人话；②**提判断**——`state ∈ {running,idle,blocked,waiting_approval,done,failed}`（§4.1 会话态）；③**提案下一步动作**——`proposals: list[ProposedAction]`，每条 = `{command, kind, rationale, expected_effect, reversible}` 正好映射 `actions` 行（§7.1），按重要性排序、可零条。**Operator 绝不自批自执行**（§4.1）——只产「提案」，每条往下走 Auditor（T4.2）→ Gate（确定性可逆性分级 §6.6）→ 决策卡（T4.3）才落子；模块只做文本→结构解析，无 subprocess/eval/shell。**从严默认（§6.7）**：解析失败/空回复 → **零提案 + `blocked` 态**（绝不从散文里凭空造命令），未知 state → blocked，无 command 的提案丢弃，proposals 非 list → 忽略；**`reversible` 保守默认 False**——仅显式 true/yes/1 才算可逆，省得未知可逆性的动作绕过 Gate 自动跑（§6.6「可回退放手、不可逆必先问」）。**§15** system prompt 追加 `language_directive`（summary 随语言）；长输出**留尾截断**（与 Reviewer 留头相反，尾巴最新最相关）控 token。三关：①Code Review（subagent）——JSON 容错抽取/reversible 强转/无 command 丢弃/留尾截断 全核对，**APPROVE**（仅 nits），按建议补「顶层 JSON 数组→fail-closed」测试；②安全（subagent，**PASS**）——无 subprocess/eval/shell、不碰/不日志 api_key、不可逆动作不可经垃圾输出变可自动跑、untrusted agent_output 仅作 prompt 文本嵌入、输出长度上界（20000 字）防 DoS；③PM 验收——对照 §6.1/§4.1：精简+提判断+提案三件事齐、映射 actions 行、不自执行、§15、从严默认 全达成。**261 tests 全绿（+23）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared）。**执行层延后**（同 T2.6/T2.7/T3.4 惯例）：Auditor 接线归 T4.2、决策卡归 T4.3、自治档位归 T4.4、真执行（检查点→跑→记）归 P4 决策回路；live LLM key mock 测+延后。
- [x] **T4.2** Auditor：§6.7 提示词骨架（两轴评分、从严默认、防自偏、verdict `pass/revise/reject/escalate`）。✅ 把 T0.2 的 `NotImplementedError` stub 换成完整实现（client/core，纯 client/core + shared）。**与 Operator/Reviewer 三件套补全**（§4.1）：Operator「该做什么」(提案)→**Auditor「该不该做」(动手前判)**→Reviewer「做得好不好」(动手后判)。**§6.7 五原则落地**：①**两层防御**——硬危险归 Gate 确定性规则，Auditor 只啃「垃圾/跑偏/过度工程」灰色地带，**绝不当唯一闸门**（AUDIT_SYSTEM 明示）；②**两轴分评（ToolEmu）**——`goal_quality∈{on-track,weak,garbage}` + `risk_severity∈{none,mild,severe}`，最后才合成 verdict；③**从严默认（"该退就退"）**——`_synthesize_verdict` **只收紧不放松**：severe→强制 escalate、garbage→绝不 pass(降 reject)；解析失败/未知 verdict/未知轴值 全部 fail-closed 到 escalate + 最坏轴（garbage/severe）；④**对着你的规矩审（GuardAgent）**——`build_audit_prompt` 喂检查清单：目标｜当前步骤｜代码规范｜QA｜可写白名单｜自治档位｜最近动作（末项抓多步组合危害）；⑤**结构化防自偏（LLM-as-judge）**——独立上下文、对抗式措辞、Operator 自辩标 `UNVERIFIED — not fact`。verdict `pass/revise/reject/escalate`；§15 `language_directive` 追加；结果映射 `audits` 行（verdict/risk_severity/goal_quality/reasons/suggestions/model）。**附带**：`Audit` 模型补 §7.1 缺的 `risk_severity`/`goal_quality` 两列（默认取**最坏值** fail-closed）。三关：①**Code Review**（subagent，**APPROVE**）——「收紧不放松」无放过路径、JSON 容错（fence/散文/顶层数组 fail-closed）、未知轴值合成前归最坏 全核对；按建议把 DB 列默认改为保守最坏值（防裸构造行 auto-safe）；②**安全**（subagent，**PASS**，无 CRITICAL/HIGH/MEDIUM）——确认 **绝不执行被审命令**（无 subprocess/eval/shell）、`model` 仅模型名非 key、untrusted 命令/rationale 仅作 prompt 文本、**garbage/空/坏 JSON 绝无法变 pass**（fail-closed 链完整）、命令无长度上界但唯一去向是用户自有 LLM 端点（接受）；③**PM 验收**——对照验收行 + §6.7：骨架/两轴/从严/防自偏/四态 verdict 逐条达成。**执行层延后**（同 T2.6/T2.7/T3.4/T4.1 惯例）：verdict→Operator(revise)/Gate+卡(escalate) 接线归 T4.3 决策卡；live LLM key mock 测+延后。**282 passed +2 skipped（+21 本任务）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared）。
- [x] **T4.3** Decision Card + 详情页下钻（原始返回 / 逐行 diff，§6.3）。✅ 决策卡的「折叠态摘要 + 一键下钻原始证据」两件套（§6.3）：①**决策卡**——`CardService.build_card`（client/core）落 `decision_cards` 行（summary 一句话 + audit_note 审核员意见 + `diff_stat` 📎改动 + `options` 一键按钮），`list_cards` 返 JSON（newest-first，可按会话过滤）；卡默认四个一键选项（✅通过/🔄让它补/⛔撤掉重来/✍️自己打，对齐 §6.3 卡样）。②**步骤详情接口** `GET /api/actions/{id}/detail`（§287 精确实现）——组装 **① 原始返回**：该步 `agent_output`/`tool_pre`/`tool_post`/`stop`/`notification` 事件，**按检查点时间窗 [本检查点, 下一检查点) 精确切片**（无检查点→该会话全部 raw 事件兜底）；**② 代码改动**：该步检查点 ↔ 当前工作区的逐文件逐行 diff——纯函数 `parse_unified_diff`（计 +/−、跟行号、新建/删除文件 `/dev/null`、二进制、`\ No newline`/index 头皆归 meta 不误计），经注入的 `CheckpointManager.diff`（argv 无 shell、`rev-parse --verify` 防注入、遵守 .gitignore 不泄机密）。③**一键定夺** `POST /api/cards/{id}/choose`——记 `chosen`+发 `card_decided` 事件（persist-first），选项服务端白名单校验（坏选项→400/未知卡→404）。**边界（§8.3/§14）**：CardService 纯 client/core，像 Gate 一样**注入** app.py（app.py 仍只依赖 shared）；diff/原始返回**只在本地进程组装**，团队 `serve` 不注入→三端点全 503，永不经总机外泄。**§6.5 衔接**：卡↔检查点同粒度——action.checkpoint_id 锚定该步 diff。**PWA**：`/api/cards`→渲染卡（模板克隆 + 全 textContent 防 XSS，摘要/审核/diff 行皆不可信 agent 输入）；🔍查看详情→`openDetail` 拉 detail→tab① 原始返回逐事件、tab② 逐行 diff 带 `.diff-add`/`.diff-del` 高亮（行号/增删一目）；选项按钮→`chooseCard` 落决策；语言切换重渲卡。**附带**：`Store._ensure_columns` 给 `decisioncard` 补 `diff_stat` 列（create_all 不改既有表，P5 迁移器前的幂等 stop-gap，仅插硬编码常量名无注入）。三关：①**Code Review**（subagent，**APPROVE**）——diff 解析计数/行号、检查点时间窗 [start,end)、`_ensure_columns` 幂等、JS XSS 安全 全核对；按建议**修空行误判为 context 致行号漂移**（裸 ""→meta 不进数）+ **补一键选项点击回路**（原按钮无 handler→inert，现 `chooseCard` 落决策）；②**安全**（subagent，**PASS**，无 CRITICAL/HIGH/MEDIUM）——确认 无 subprocess/shell/eval、git 仅吃服务端派生的 commit SHA（非请求输入）经 `rev-parse --verify`、raw SQL 仅插硬编码表/列名无注入、**本地专属边界**（团队 serve→503 不外泄 diff/原始返回）、PWA 全 textContent 无 innerHTML、无路径穿越/SSRF、端点鉴权与既有 /api/* loopback 信任模型一致（非新暴露）；③**PM 验收**——对照 §6.3/§286-287/§293-294：决策卡折叠态 + 🔍下钻两标签（原始返回/逐行 diff）+ `GET /api/actions/{id}/detail` + 边界 全达成。**Playwright/chromium 浏览器验收通过**：渲染决策卡（💬摘要+🕵️审核+📎2文件+3/−1+四选项）→点🔍查看详情→tab① 渲染原始 agent_output 事件→切 tab② 渲染 auth.py +1/−1 与 new.py +2/−0 逐行高亮（3 加 1 删）；另测点✅通过→`chosen=approve` 落库；双截图留证。**302 tests 全绿（+20 本任务：cards 单测/API + web 接线 + choose 回路）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared，注入式）。**执行层延后**（同 T2.6/T2.7/T3.4/T4.1/T4.2 惯例）：卡选项→真执行（检查点→跑/nudge/一键回退）需 Runner 两向控制（send/interrupt，P4），`card_decided` 已标 `execution_deferred=True`；卡的 Operator+Auditor 自动生成接线归 T4.4 决策回路串联；live LLM key mock 测+延后。
- [x] **T4.4** 自治档位（0/1/2/3，默认 1）。✅ 决策回路的「松紧旋钮」落地（§6.4/§6.6）：**能力恒满，落子松紧由档位定**。新增 **shared `autonomy.py`**（放 shared 而非 client/core——因 server `app.py` 仅依赖 shared 也要用 §14 边界）：①`normalize_level`（任意输入→0..3，默认 1，越界钳制，str/int/None/垃圾兜底，config_kv 存字符串故必须收 str）；②**纯策略** `decide_disposition(classification, level)→auto|card|report` 把 Gate 三档分级 × 档位映射成处置：**档0** 全 report（不自落子只汇报）、**档1** 全 card（凡事都问，默认）、**档2** safe→auto 其余→card、**档3** safe+needs-strategy→auto 仅不可逆→card；**🔑红线（§6.6）**：requires-approval（及任何未知/坏分级，fail-closed）**任何档位都绝不 auto**——undo 救不回的只能事前问；③`level_label(level, lang)` 双语一句话描述，喂 Auditor 提示词的「自治档位」槽（Auditor.audit 已接 `autonomy: str`，提示词已追加 language_directive §15）。**Gate.disposition(action_text, level)**：classify+dial 一次调用，决策回路每个提案调一次（执行 auto/card 仍延后 P4 两向控制）。**config** 加 `AutonomyCfg(level=1)` 基线，**config_kv `autonomy.level`** 运行时覆盖（同 ui.language 模式）。**端点**（app.py 纯 shared，store 鸭子注入）：`GET /api/settings/autonomy`（config_kv 覆盖否则 config 基线，返 level+双语 label；无 store 也能读基线）、`POST`（钳制后存，无 store→503）。**UI**：header 加自治档位下拉（0/1/2/3）+ i18n + 同步后端 + label 作 title 悬浮提示。三关：①**Code Review**——决策矩阵 4×3 全覆盖、unknown 类 fail-closed 不入 auto 道、level 钳制、cls 单次计算（去冗余）、shared 不 import client（边界）皆核对；②**安全**——无 subprocess/shell/eval、**安全不变式「不可逆永不 auto」单测验证**、POST 仅存 `str(int(clamp))` 经参数化 config_kv 无注入、端点沿用 /api/settings/language loopback 信任模型（team serve→503 不泄漏）、UI select 数值静态+label 走 .title 无 XSS；③**PM 验收**——对照 §6.4：四档 + 默认 1 + 处置矩阵 + §6.6「可逆放手不可逆必问」逐条达成；**Playwright/chromium 浏览器验收通过**（默认档位渲染为 1→选 3→`POST /api/settings/autonomy` 落库 level=3→刷新仍读回 3，header「自治 [3]」截图留证）。**316 tests 全绿（+14：autonomy 单测/矩阵/端点/Gate 集成 + web 接线）+ ruff 通过**。client⊥server 边界守住（dial 逻辑纯 shared、Gate 纯 client/core+shared、app.py 纯 shared）。**执行层延后**（同 T2.6/T2.7/T3.4/T4.1/T4.2/T4.3 惯例）：disposition→真执行（auto 直跑检查点→落子 / card→弹卡 / report→仅记）需 Runner 两向控制（send=P4 stub / interrupt=P3 stub），归 T4.6 决策回路串联；live LLM key mock 测+延后。
- [x] **T4.5** Operator Toolbelt（§4.7）：截屏(含鼠标渲染选项)/鼠标/键盘/管理员 shell；computer-use 会话侧执行。✅ 把 `client/computer_use` 占位换成完整执行层 `toolbelt.py`（纯 client，无 server/shared 泄漏，§14 边界）——Operator（§6.1）的「手」，跑在用户交互会话里（PC 是普通会话应用、非 Windows 服务→无 Session 0 限制，§3.1）。**能力给满**：①**截屏**`screenshot(cursor=hide|show|highlight, region=)`——**鼠标渲染选项**（你点的需求 §4.7）：HIDE（干净画面/指针挡内容）/SHOW（原样小点）/HIGHLIGHT（指针处画光圈+红点，给你看「正要点哪儿」/录演示）；默认 `self_cursor=HIDE`（Operator 自看少遮挡），非法值回退 self_cursor；只读 `safe` 永不拦；②**鼠标**move/click/drag、③**键盘**type/hotkey、④**shell**`run_shell(command, admin=)`。**§4.7 安全链落地（不是唯一闸门 §6.7①）**：`capability_risk` 把每个能力映射成 Gate 风险词汇——截屏/移动=`safe`、点击/拖拽/打字/快捷键=`needs-strategy`(灰色，GUI 不可逆动作[删/发/付]测不准→交 Auditor/卡/档位判)、**管理员 shell 永远 `requires-approval`**（UAC/sudo 一旦跑常 undo 不回，§4.7）、shell 命令文本交 `Gate.classify`（rm -rf/git push 在那拦）、未知能力 fail-closed→requires-approval。**从严默认/防误执行**：requires-approval 能力**不带 `approved=True` 绝不碰后端**（测试验证 backend.calls==[]）——即便调用方有 bug 也无法让 Toolbelt 跑提权命令。**后端注入**（鸭子类型 screen/mouse/keyboard/shell）全可单测无真 GUI；默认真后端 **懒导入** mss/Pillow（截屏+光圈合成）、pyautogui（鼠标键盘）、subprocess（**argv 列表 no shell** 防注入），导入本模块永不要求这些包（验证 import 后 mss/pyautogui/PIL 不在 sys.modules）。错误兜底：后端抛错→`ok=False`、消息仅 `类型: str[:200]`（非全 repr，防路径/机密泄漏）；截屏原始 PNG 放 `image` 字段 repr=False、不进 JSON 友好的 `data` 面。**三关**：①Code Review——分类矩阵全覆盖+fail-closed 后端零触+懒导入+边界+ToolResult.image 正规字段（非 monkey-patch）+ argv no shell 皆核对；②安全——无 shell=True/eval/注入、提权与危险命令无审批不可跑（唯一可自动的 shell 是 Gate 判 safe 的）、admin 提权执行**显式 raise 延后**（绝不静默降权跑）、错误消息截断防泄漏、截屏像素不入事件/卡 JSON 面；接受并标注的残留风险：approved=True 时信任调用方「审批↔命令」绑定（重绑属决策回路 T4.6）、live GUI/提权延后、多模态成本按需截不连拍(§13.6)；③PM 验收——对照验收行+§4.7：截屏(三种光标渲染)/鼠标/键盘/管理员 shell + 会话侧执行 + 安全链(分级+fail-closed)逐条达成。**340 tests 全绿（+24 本任务）+ ruff 通过**。**执行串联延后**（同 T4.1-T4.4 惯例）：能力→真 Auditor→Gate→决策卡→落子的决策回路串联 + 决策回路调 `plan()` 取 ToolCall 审/卡/执行 归 **T4.6**；live 桌面（真 mss/pyautogui 截屏点击）+ 真 UAC/sudo 提权需带 GUI 的桌面环境验证（注入后端路径已全单测）。client⊥server 边界守住（纯 client/computer_use）。
- [x] **T4.6** 手机下发任务 + 多会话 + 简报。✅ 把决策回路的「手机面三件套」落地（§5.1/§5.5），延续 Gate/CardService 的注入式边界（app.py 仍纯 shared，client/core 注入，§14）。①**手机下发任务**（§5.1）——新增 `DispatchService`（client/core）：`POST /api/tasks` → `create(goal, workspace?, agent?)` 校验（空目标/无库/未知 agent/工作区不在白名单 fail-closed）→ 复用 `build_session_task` 建 Root Session+Task → persist-first 落 `dispatch` 事件上总线 → **可注入 launcher**（local_app 接真 Runner，手机一点真起 agent、后台并发；无 launcher→`execution_deferred=True`，沿用 P2–P4 live 延后惯例）；`goal` 截 8000 字防 token 爆；fire-and-forget 任务持强引用防 GC、launch 失败只记 `error` 事件不拖垮 server loop。②**多会话**——`GET /api/overview` 汇总每个会话（事件数/最近事件/未决卡/待审批），新→旧排序，Runner 早已支持并发（T1.7）；PWA 会话列表渲染活动计数。③**简报**（§5.5）——新增 `BriefingService`（client/core）：`generate(session_id?, kind)` 用**你的 LLM**（config.llm，httpx mock 测）把会话近况（或全会话花名册）→ 结构化 `{title, body_md}` → 落 `reports` 表 + persist-first `briefing` 事件 + best-effort Web Push（GONE 端点剪枝）；§15 system prompt 追加 `language_directive`；解析失败优雅降级（简报是信息非安全闸，不 fail-closed）；`GET /api/reports` + `POST /api/reports/generate`。库补 `add_report`/`get_reports`。**三关**：①Code Review（subagent，无 high）——按建议修：`_push` sent 判定改 endpoint 集合（去脆弱长度比）、fire-and-forget 持引用、`_gather_all` 去 N+1、删 JS 死变量、overview fetch 抛错也回退 /api/sessions；②安全（subagent，无 critical/high）——修 MEDIUM：**显式 workspace 必须落在 cfg.workspaces 白名单内**（否则 `workspace_not_allowed`/400），防经 API 指定任意 cwd 起 agent（§6.6 白名单）；`goal` 截断防 DoS；确认 无 shell（`create_subprocess_exec` argv，agent 名限 enabled→固定 adapter）、SQLModel 参数化无注入、无机密入事件/简报/响应、PWA 全 textContent 无 XSS、无新 SSRF、鉴权与既有 /api/* loopback 信任模型一致；③PM 验收——对照验收：下发(§5.1)/多会话/简报(§5.5)三件齐，**Playwright/chromium 浏览器验收通过**（表单下发→新会话进多会话列表[既有+新建各带计数]→点生成简报→「夜间进展」卡渲染，截图留证）。**373 tests 全绿（+33：dispatch_service/briefing/API + 工作区白名单）+ ruff 通过**。client⊥server 边界守住。**Live 延后**（已标注）：手机真起 claude/codex 需机器装 CLI（launcher 已接 Runner，CI 不实跑）；简报真 LLM 需用户 key（mock 测）；真 Web Push 需 VAPID 凭据（fake pusher 测）。**执行串联**（卡选项→真 nudge/interrupt+resume/检查点执行的 Operator→Auditor→卡→检查点→执行决策回路）归 P4 验收行（Runner.send=P4 stub / interrupt=P3 stub）。
- [x] **验收**：一步走完 Operator→Auditor→卡→你点→检查点→执行；全程手机可操作。✅ 把 T4.1–T4.6 一路延后的「执行层 / 两向控制」真正落地，新增决策回路串联 `DecisionLoop`（client/core，§6.2）+ **Runner/adapter 两向控制**（`send`=`--resume` 续接、`interrupt`=终止；claude/codex `_build_resume_cmd`；argv 无 shell）。**串联**：`observe(goal, agent_output, level)` → Operator 提案 → 逐提案 Auditor 审（落 `audits` 行）→ 按 verdict×档位定夺：reject→丢弃 / revise→`Runner.send` 退回让它补 / escalate→必弹卡 / pass→`Gate.disposition`(classify×dial)→ **auto**(检查点→执行) / **card**(弹卡等你点) / **report**。**你点→检查点→执行**：`CardService` 注入 `executor=loop.on_card_decision`，`POST /api/cards/{id}/choose` 真正落子——approve→`execute_action`(**先打真 git 检查点**`snapshot`→经 Toolbelt 跑命令→记 `executed_at`/`checkpoint_id`+发 `action_executed`) / undo→`undo_to` 逐字节回退到该步检查点(§6.5②) / revise→退回 / manual→不自动跑。**全程手机可操作**：看卡(`GET /api/cards`)→下钻证据(`GET /api/actions/{id}/detail` 逐行 diff)→一键定夺(`POST /api/cards/{id}/choose`)全走手机打的 FastAPI 面。**新事件类型** `action_executed`/`action_undone`；store 补 `update_action`/`add_audit`/`get_audits`。三关：①**Code Review**（subagent）——状态机 reject/revise/escalate/pass×auto/card/report 全核对、CardService 无 executor 时**严格保持** `{ok,id,chosen}`+`execution_deferred=True` 旧契约（旧测试精确通过）；**按 HIGH 修**：undo 的 redo 快照走 git refs 而 `_checkpoint` 原走 DB 步号→**步号不一致会覆盖 redo ref**，改 `_checkpoint` 用 `CheckpointManager.next_step`(git 单一真相源)，加回归测试锁定；**按 MED 修**：`Runner.send` 重 pump 前先 `cancel` 旧 pump、`interrupt` 也取消 pump、补 `Runner.stop` 清四张表防长跑泄漏。②**安全**（subagent，无 critical/high）——确认 唯一 OS 执行是 Toolbelt `subprocess.run(argv)` 无 `shell=True`、resume 命令 argv 分离无注入、**红线**(requires-approval/未知分类任何档位绝不 auto)经 `decide_disposition` 闭集守住、坏 LLM 输出 fail-closed(未知 verdict→escalate/未知档→1/未知能力→requires-approval)、事件/卡不带 secrets/stdout/路径、`_emsg` 截断、client⊥server 边界(loop 纯 client/core，仅经 `cards.executor` 注入，app.py 未改纯 shared)；**按 MED 修空默认网关风险**：`GatesCfg.requires_approval` 原默认空→无 config.yaml 时一切判 safe、配合新 auto 执行=红线失守，**烘焙 18 条不可逆 denylist 默认**(git push/rm -rf/drop table/sudo/runas/mkfs…)做兜底；**按 LOW 修**：Operator `reversible=False` 提示作**只收紧不放松**的兜底(即便 Gate 没拦也强制弹卡)、`mcp_tool`/`file_edit` 不再被当 shell 误跑(交各自后端，GUI 保留自己的风险分级 §4.7)。③**PM 验收**——对照 ROADMAP P4「Done when」逐条有据：一步走完五段链 + 检查点真打在执行前(逐字节可回退) + 全程手机可操作，且**阶段级集成测试** `tests/test_p4_acceptance.py`（3 例，真实 Store+EventBus+**真 git 工作区**+真 FastAPI TestClient+真 Toolbelt[仅 OS shell 注入假]+真 CheckpointManager；Operator/Auditor 假，live LLM 归 T4.1/T4.2 mock）串端到端：①一步走完+手机可操作(看卡→点 approve→检查点+执行+逐行 diff)②两向控制真驱动 agent(`agent_instruction`→`Runner.send`、revise→退回)③不可逆红线(级 3 `git push` 仍弹卡不自动)。**392 tests 全绿（+19 本任务：decision_loop 11 + runner 两向控制/resume 5 + P4 集成 3）+ ruff 通过**。**Live 延后**（已标注，同 P2–P4 惯例）：真 claude/codex `--resume` 续接 + 真桌面 shell 执行需带 CLI/GUI 的机器（注入 seam 已全单测）；live LLM key 归 T4.1/T4.2 mock。**P4 阶段完成**。client⊥server 边界守住。

## P5 — 定义引擎（⭐ 秘方层）
- [x] **T5.1** `definitions` + `definition_links` + `workflow_runs` 表。✅ 三张「秘方」表的本地 Store r/w 助手落地（client/store/db.py，纯 client + shared，§11.2/§7.1；服务器库**绝不**含这些 §8.3/§14）——模型此前已建（table=True），本任务补齐读写：①**definitions**（一张表装四种 kind=workflow|skill|code_standard|qa_rubric，加 kind 即扩展不改表 §11.2C）——`add_definition`（盖 created_at/updated_at + **`(kind,name,version)` 唯一性守卫**，重复版本 raise ValueError 逼调用方升版本，per-kind 隔离；逻辑守卫 TOCTOU 对单进程本地 sqlite 可接受，同 push-sub upsert 既有风格）/`get_definition`/`get_definitions`（按 kind/name 过滤 + active_only + 按 kind,name,version 排序）/`get_active_definition(kind,name)`（当前启用版）/**`set_definition_active`**（启用某版→**同(kind,name)其余版自动停用**，恒「正好一个 live」= §11.2 启用/停用/回滚旋钮；身份映射使 row 与 sibling 同对象、幂等重激活零冗余写）/`update_definition`（就地改 body/scope/metadata/status 不动 name/version/kind 身份、bump updated_at，P6 UI 编辑器用）；②**definition_links**——`add/get_definition_link(s)`（按 from_id 取、可按 relation/step_index 收窄、按 step→relation 排序）/`delete_definition_link`（幂等）把「工作流第 N 步→用哪块积木」连起（uses_skill|uses_standard|judged_by）；③**workflow_runs**——`add_workflow_run`（盖 started_at）/`get_workflow_run(s)`（按会话取、按 step_index 排序）/`update_workflow_run`（推进 step_index/step_status pending→running→qa→passed|failed|blocked、盖 ended_at，仅改传入字段）记某会话按某工作流跑到第几步。三关：①**Code Review**（subagent，**APPROVE**）——set_definition_active 恒一活/身份映射别名良性/幂等、唯一守卫 per-kind、排序过滤、偏更新不clobber 全核对，无 bug；server store 无可复用（按设计本就不含秘方）；②**安全**（subagent，**PASS**）——全 SQLModel 参数化无注入、**body 等秘方内容仅存取从不执行**（无 shell/eval/subprocess）、grep 确认 `server/` 不 import `foreman.client.store`（边界守住 §8.3/§14）、无 secrets 入日志/返回、秘方无网络/序列化出库路径；③**PM 验收**——对照 §11.2/§7.1：三表读写齐 + 四种 kind 单表 + 版本启用/停用/回滚 + 逐步积木连线 + 逐会话运行追踪 全达成。**406 tests 全绿（+14 本任务：tests/test_definitions_store.py）+ ruff 通过**。client⊥server 边界守住（纯 client/store + shared，无需部署/无 UI）。
- [x] **T5.2** 混合式工作流引擎（固定骨架 + 每步 LLM/skill 驱动 + 卡审批点）。✅ 新增 `client/core/workflow_engine.py`（纯 client/core + shared，§14 边界）落地 §11.2「骨架是死的、每步干法是活的」：①**固定骨架**——`parse_workflow(body)` 把 `definitions(kind=workflow).body`（YAML/JSON，§585）解析成 `WorkflowSpec{steps[]}`，**从严默认（§6.7）**：空/坏 YAML/非映射步骤/无步骤 一律 `error`+**零步骤**（绝不从垃圾凭空造骨架）；用 `yaml.safe_load`（非 `yaml.load`，杜绝恶意 body 反序列化执行）；②**每步 LLM/skill 驱动**——`_resolve_material` 把该步要用的**技能 + 代码规范**（名字来自步骤内联 *和* `definition_links` 关系表 uses_skill/uses_standard/judged_by，order-preserving 去重）解析成各自**启用版** body → 拼成 `injected_md`（「前面教」的注入素材，§11.2D；真写进工作区 CLAUDE.md/AGENTS.md 是 **T5.3**，本任务产素材+留缝）；解析不到的块记进 `missing` 绝不静默丢；③**卡审批点**——`approval:true` 步骤经注入的 `CardService` 建决策卡（背一条 no-op 合成 `workflow_gate` action，即便误入决策回路执行器也 defer 不当 shell 跑）+ 把 run 标 `blocked`，`resume_after_gate(approved)` 续（批→进、驳→failed）；④**状态机**靠 `workflow_runs`（T5.1）：`start→begin_step(running)→submit_step`（gate→blocked / 有 QA 无结果→park `qa` 把 rubric 交 **T5.4** 评 / qa_passed=False→failed / 否则 `_advance`）→末步 `passed`+ended_at；`blocked` 态 submit 拒绝（防二次弹卡）；状态转换发 persist-first `workflow` 事件（**新增事件类型**，§7.1 枚举补上）。**QA 驱动审阅本体延后 T5.4**（本引擎用调用方给的 `qa_passed` 只做状态流转，rubric 文本已解析备用）。三关：①**Code Review**——骨架解析 fail-closed/链+内联合并去重/状态机各分支/末步收口 全核对；自审修：`blocked` 态 submit 早退（防 gate 二次 card）；②**安全**——grep 确认 无 subprocess/shell/eval/`os.system`、**仅 `yaml.safe_load`**、全 DB 走 SQLModel 参数化无注入、无 LLM key/无网络、事件载荷仅元数据（run_id/phase/step/名字，不含 body/路径/机密）、import 仅 `shared`+`store.models`（server 未碰、秘方只在本地库 §8.3/§14）、合成 gate action 是 no-op kind 永不误执行；接受并标注：工作流审批闸是**叠加**在确定性 Gate（真红线 §6.7①）之上的补充闸，`approval` 坏布尔默认 False 属用户自有可信秘方的编辑笔误而非攻击路径；③**PM 验收**——对照验收行+§11.2：固定骨架(可复现) + 每步技能/规范注入素材 + 卡审批点(建卡+阻塞+续跑) + 逐会话运行追踪 全达成。**431 tests 全绿（+25：tests/test_workflow_engine.py 解析/素材/链/状态机/gate）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared，无 UI/无部署，同 T5.1）。
- [x] **T5.3** 事前注入工作区（CLAUDE.md / AGENTS.md / skill）。✅ 把 T5.2 留的「产素材→写工作区」缝补上（§11.2 D「前面教」的事前注入）：新增 `client/core/injector.py` `WorkspaceInjector`（纯 client + 文件系统，无 server/shared 泄漏，§8.3/§14）——启动 claude/codex 前把这一步要用的**技能 + 代码规范**写进工作区：①**CLAUDE.md**（Claude Code 读）/ **AGENTS.md**（codex 读）——本步指令 + 每条代码规范放进**受管标记块**（`<!-- FOREMAN:BEGIN/END -->` HTML 注释，渲染不可见），**只占标记块内**、块外用户自有内容原样保留；②**技能**（「做法手册」）各写成 `.foreman/skills/<slug>.md` 单独文件，受管块里挂链接（指引文件保持精简、详手册落盘）；③**可逆**——`clear()` 抹掉标记块（只我们建的文件整删）+ 删 `.foreman/skills`，工作区还原（AI 脚手架不渗进用户 commit/diff）。**安全（§6.6/§6.7）**：技能名→文件名经 `_slug`（denylist `[^0-9A-Za-z._-]+`→`-` + strip 首尾点划，`/`/`\` 必被替换→`../../etc/passwd`→`etc-passwd.md` 永逃不出 skills 目录、`..`→空跳过并记 skipped）；可选 `allowed_roots` 白名单（防御纵深，叠在 dispatch 层白名单上）；标记块 upsert/strip 用 `_block_span`（首 BEGIN→末 END）**自愈**——半写/重复块折叠成单块、不残留脏脚手架。**接线**：`WorkflowEngine` 加注入 `injector`，`begin_step` 启动步骤前注入（best-effort、写失败/无工作区只记不崩）、终态（PASSED/FAILED/gate 驳回）`_clear` 还原工作区。**§15 N/A**（纯文件 I/O 无 LLM 文本组件）。三关：①**Code Review**（subagent，**APPROVE-WITH-NITS**）——slug/防穿越、标记块 upsert/strip 幂等保留用户内容、引擎 best-effort 接线 全核对；按建议**修标记块首 BEGIN→末 END 自愈**（防崩溃半写残留）+ rmtree 失败不误报 removed；②**安全**（subagent，**PASS-WITH-NOTES**）——逐 payload 验证**路径穿越不可达**（slug 去所有分隔符、NTFS ADS `:`→`-` 中和）、无 secrets 写盘（body 来自用户自有本地 definitions）、无 shell/eval/subprocess、client⊥server 边界干净、块外用户内容不被覆盖；残留 LOW（嵌套符号链接 rmtree、用户文件恰含标记串）属「可信工作区」威胁模型接受并加注释；③**PM 验收**——对照 §11.2 D：启动前注入技能+规范到 CLAUDE.md/AGENTS.md/skill 文件 + 可逆 + 引擎接线 全达成。**447 tests 全绿（+15 本任务：tests/test_injector.py 13 + 引擎注入接线 2）+ ruff 通过**。client⊥server 边界守住（纯 client/core + 文件系统）。无 server/UI 故无需部署/浏览器验收。
- [x] **T5.4** QA 标准驱动审阅。✅ 把 T5.2 留的「QA 闸由调用方给 `qa_passed` 只做状态流转」补全成真审阅：新增 `client/core/qa_review.py` `WorkflowQAReviewer`（纯 client/core + shared，§14 边界）落地 §11.2 step 3「每一步干完，引擎拿这步的 QA 标准让 AI 审一遍，**过了才走下一步**」。**回路闭合**：引擎（T5.2）把带 QA 标准的步骤 park 在 `qa` 态 → `review_step(run_id, *, goal, diff, context, from_ref, to_ref)` ①取该步**已解析的 QA 标准 body**（尺子「怎么算合格」§88-89，经 `engine.step_view` 拿 active rubric）+ ②取该步**改动 diff**（显式 diff 优先；否则经注入的 `CheckpointManager.diff(from_ref)` 算——含新建未跟踪文件，argv 无 shell、`rev-parse --verify` 防注入、遵守 .gitignore 不泄机密；坏 ref 降级空串不崩，同 `_inject` best-effort 惯例）+ ③goal（显式>会话目标>步骤指令）→ 喂**你的 LLM Reviewer**（T2.7，复用 §15 language_directive）→ **verdict→pass/fail 映射**：**从严默认（§6.7）只有 `approve` 才 pass→advance**，`request_changes`/`escalate`/解析失败escalate 一律**不放行**（engine 标 `failed` + 还原注入脚手架 T5.3）→ 回喂 `engine.submit_step(qa_passed=…)`。**fail-closed 守卫**：no_run / not_in_qa（只审 park 在 QA 的步）/ no_qa_rubric（rubric body 解析不到绝不静默放行）。**escalate→人工**：`needs_human` 经返回体上抛供决策回路弹卡（⛔撤掉重来 §5.3），执行层延后（同 P2–P5「执行层延后」惯例——决策回路两向控制）。**事件**：persist-first `review` 事件（复用既有类型，source=`qa-reviewer`，**仅元数据**：run_id/step/rubric名/verdict/qa_passed/needs_human/summary，**diff 内容绝不入载荷**，sentinel 测试验证）。三关：①Code Review——verdict 映射 fail-closed（只 approve 放行）、None-safe（store/session/run）、begin_step 同步 vs submit_step 异步处理、事件元数据-only 全核对；②安全——无 subprocess/shell/eval、diff 走既有 CheckpointManager（argv/rev-parse --verify/.gitignore）、事件不泄 diff/机密、grep 确认 server 不 import 本模块（边界守住 §14）、LLM key 由 LLMClient 管不外泄；③PM 验收——对照 §11.2 step 3 + §88-89：取 QA 标准→LLM 审→过了才走下一步 + 尺子驱动 + §15（e2e mock-LLM 测验证简中指令 + rubric/goal/diff 三者入提示词）+ fail-closed 全达成。**457 tests 全绿（+10：tests/test_qa_review.py 含 verdict 映射/输入传递/fail-closed 守卫/事件/真 Reviewer mock-LLM 端到端）+ ruff 通过**。client⊥server 边界守住（纯 client/core + shared，无 server/UI 故无需部署/浏览器验收，同 T5.1–T5.3）。**Live LLM 延后**（用户 key，mock 测，同惯例）。
- [x] **T5.5** 数据库迁移（schema_version + 迁移器）。✅ 把「升级后你的历史还在」（§11.1 point 3）落地为**账本式幂等迁移器**（`shared/migrations.py`，两端共用 §14 边界）：`create_all` 只建**缺的整张表**（新装/新表），迁移器补它做不到的**就地改表**（ADD COLUMN/回填）并把每个已应用版本记进 `schema_version` 账本（**一版一行**，兼当审计轨迹 §7.1）。**核心安全网=幂等**：`add_column`/`column_exists`/`table_exists` 在变更已存在时 no-op，故同一迁移在**全新库**（create_all 已建最新列→no-op）和**老库**（真 ADD COLUMN）皆安全、半途崩溃后**可续跑**（每迁移独立事务、版本仅在 upgrade 成功后才入账本；pysqlite 不回滚 ALTER，所以续跑安全**靠幂等而非事务性 DDL**，已在 docstring/测试讲明）。`run_migrations` 升序应用、跳过已入账本版本、重复版本号 raise、返回本次应用清单。**接线**：`Store.init()`/`ServerStore.init()` 改为 create_all→run_migrations（退休 client 的 `_ensure_columns` 临时补丁，把它正式化为 client v1 迁移=decisioncard.diff_stat §6.3）；server 迁移 v1 基线 no-op + **v2 加 accounts.password_hash**（T3.5 延后到此的真迁移：老 v1 server 库的 accounts 行平滑补列；auth_sessions 是整张新表由 create_all 管）。各加 `schema_version()` 读当前版。**历史命名痣**（保留向后兼容）：client 账本表名沿用 `schemaversion`（其 `SchemaVersion` 模型默认表名），server 用 `schema_version`（§7.1）——迁移器经 `version_table=` 参数各打各的表，与各自 ORM 模型读写同表（统一表名属可选后续，避免改名造成元数据冲突/孤立既有本地库）。三关：①**Code Review**（subagent，**APPROVE**）——升序/去重/成功才入账本/续跑、client⊥server 表名一致（init 落账与 schema_version() 与既有 test_*_store 的 `get(SchemaVersion,…)` 同表）、全新库 no-op+老库加列两路幂等、v1 基线对线上单行 version=2 库的回填安全（应用 v1 no-op 补 {1}→{1,2}、跳过 v2、不动数据）、移除 SCHEMA_VERSION/_ensure_columns 无残留 全核对，无 bug；②**安全**（subagent，**PASS**）——DDL f-string 的 table/column/decl/version_table **全是代码内字面量**绝非请求数据（无注入）、**仅增量 ADD COLUMN/CREATE IF NOT EXISTS**（无 DROP/DELETE 故不丢数据）、无 shell/eval/网络/反序列化、不碰行数据/不记机密、server 不 import client（边界）；③**PM 验收**——对照 §11.1：结构版本号 + 自动跑迁移 + 老数据平滑升一条不丢 全达成，**集成测试证**老 `decisioncard`(无 diff_stat)/老 `accounts`(无 password_hash) 升级后补列且不动行。**471 tests 全绿（+14 本任务：test_migrations.py 10 + test_store_migrations.py 4；另改 2 既有 store 测试断言为账本语义）+ ruff 通过**；server `/health` 本地 200。**P5 阶段完成**。client⊥server 边界守住（迁移器在 shared、两端注入各自迁移清单）。

## P6 — UI 编辑器 + 扩展口
- [x] **T6.1** 手机/网页里增删改 工作流/技能/规范/QA。✅ 把 P5 的「秘方」从只读变可编辑（§11.2C「在手机/网页 UI 里改，直接存库，不碰文件、不用重新部署，数据库就是唯一真相源」）：新增 `client/core/definition_service.py` `DefinitionService`（纯 client/core + shared，像 Gate/CardService 一样**注入** app.py，§14 边界——秘方只在本地库、共用服务器绝不持有 §8.3）落地四种 kind（workflow/skill/code_standard/qa_rubric）的**增删改查 + 启用/回滚**：①**增** `create_definition`（**版本自动 +1**，免撞 T5.1 的 (kind,name,version) 唯一守卫；`activate` 默认 True 使新版即 live）②**改** `update_definition`（就地改 body/scope/metadata/status，**身份 kind/name/version 不可改**——改身份请新建版本）③**删** `delete_definition`（连带删 from/to 指向它的 links、幂等；新增 Store `delete_definition` 助手）④**查** list/get ⑤**启用/回滚** `activate_definition`（恒「正好一个 live」§11.2 旋钮）。**从严默认（§6.7）**：`bad_kind`（未知 kind 拒绝防 typo 造孤儿）/`bad_name`（空/超 200 字）/`body_too_large`（>200k 防 DoS）/`bad_scope_json`·`bad_metadata_json`（必须是 JSON 对象）/`version_exists`(409)/`not_found`(404)/`no_store`(503) 全 fail-closed。**审计轨迹**：每次变更发 persist-first `definition` 事件（**新增事件类型** §7.1，**仅元数据 action/id/kind/name/version，body 秘方绝不上总线**）。**REST**（app.py 纯 shared，service 注入；无 service→503＝团队缓存服务器不serve秘方）：`GET /api/definitions?kind=&name=&active_only=`、`GET/POST/PATCH/DELETE /api/definitions[/{id}]`、`POST /api/definitions/{id}/activate`；错误码→HTTP 映射齐备。**PWA**（index.html+app.js+app.css）：「秘方」区＝kind 过滤下拉 + 列表（每条 kind·name·v# + ✓live 标 + 编辑/启用/删除）+ 增/改表单（kind/name/scope/body/保存即启用，**编辑锁身份**[kind/name disabled、PATCH 只发 body/scope]，删除 confirm 二次确认）+ **全 textContent 防 XSS**（name/body 是用户秘方按不可信渲染）+ zh/en i18n + 切换语言重渲。**三关**：①Code Review（subagent，无 high）——CRUD 逻辑/版本自增/恒一活不变式/fail-closed 校验/错误码映射/delete 连带 links 幂等/编辑锁身份/XSS textContent 全核对，APPROVE；按 MED 修：编辑时「保存即启用」默认 unchecked（避免「勾选已 active 行→取消勾选无效」的误导语义，启用是单向「把这版设为 live」无 deactivate 概念）+ 删 `_validate` 里恒真的冗余 `(scope or "{}") and` 守卫；②安全（subagent，无 critical/high）——确认 SQLModel 参数化无注入、**body 秘方只存取从不执行**（无 shell/eval/yaml.load）、PWA textContent 无 XSS、`definition` 事件仅元数据不泄 body/机密、边界守住（app.py 不 import client、server 缓存库无 definitions、无 service→503）、输入尺寸上界防 DoS、鉴权沿用既有 /api/* loopback 信任模型无新暴露；③PM 验收——对照验收行 + §11.2C：手机/网页增删改四种秘方 + 直接存库不碰文件 + 数据库唯一真相源 + 启用/回滚 全达成。**Playwright/chromium 浏览器验收通过**（9/9：空态→新建工作流 add-feature v1→列表现并标 ✓live + 落库 active→编辑锁身份[name disabled]→改 body 加 lint 落库→删除[confirm]→列表回空 + 库清空；截图留证）。**503 tests 全绿（+32：test_definition_service.py 24 + test_definitions_api.py 8；含 store delete_definition 连带 links 测）+ ruff 通过**。client⊥server 边界守住。
- [x] **T6.2** 定义导出/备份 + 可选 body 加密。✅ 把「秘方」做成可携带可灾备 + 可选落库加密（§11.2C/§765「连 .db 被偷也只是一堆密文」）：①**可选 body 加密**——新增 `shared/crypto.py` `BodyCipher`（Fernet=AES-128-CBC+HMAC-SHA256 认证加密，`cryptography` **懒导入**故 import 本模块永不要求该包）：`fenc:v1:` 标签使加密**可选且可逆**——`decrypt` 对未标记值**透传**（明文行 / 加密前写的旧行原样 round-trip）、`encrypt` **幂等**（已标记→原样返回，双加密不腐坏）；`from_key`（高熵 Fernet key，首选）/`from_passphrase`（PBKDF2-HMAC-SHA256 200k 迭代，同口令跨重启可解）/`generate_key`；**密钥来自 `.env`(`FOREMAN_DEFINITION_KEY`) 绝不进 config.yaml/git**（config 加 `Secrets.definition_key`），空=不加密(默认、向后兼容)。`Store(cipher=)`：写时 `maybe_encrypt`、读时 `_decrypt_row`（get/get_all/active/update/set_active 全路径），**对所有读者(引擎/注入器/QA审阅/UI编辑器)透明**；`add_definition` 在 dup-raise 与成功路径**都把入参对象还原明文**故调用方永远拿明文。加/解密**只在此处、body 绝不执行**。②**导出/备份**——`DefinitionService.export_bundle(encrypt=)` 把全部 definitions+links 导成一个带 `format/version/exported_at/encrypted` 信封的 JSON 包（links 只含 id/relation 无 body 故恒安全）；`encrypt=True` 用 cipher 加密每条 body（无 cipher→`no_cipher`），可在机器间携带不泄秘方。新增 Store `get_all_definition_links`。③**导入/还原**——`import_bundle` **merge 语义幂等**：按 id 与 (kind,name,version) 双查重→已存在则跳过（重导永不 clobber 活跃秘方）；保留原 id 故 links 不断；加密包用 cipher 解密回明文，**fail-closed**（缺钥`needs_key`/坏钥`bad_key`，绝不落不可读密文）；**从严校验**：bad_bundle/bad_format/unsupported_version/too_large(>1万条) + 未知 kind/缺 id/悬空 link 跳过 + **MAX_BODY 上限**(超→跳，同 create) + scope/metadata 非 JSON 对象/未知 status **消毒回默认**；**重申单活不变式**：导入的 is_active 版本经 `set_definition_active` 重走一遍故每 (kind,name) 恒「正好一个 live」。审计 `definition` 事件仅元数据(counts)不含 body。**REST**（app.py 纯 shared，service 注入，团队缓存服务器无 service→503）：`GET /api/definitions/export?encrypt=`、`POST /api/definitions/import`——**路由顺序**置于 `/{definition_id}` 之前防 "export" 被当 id 捕获（有专测）。**PWA**：秘方区加 ⬇导出（Blob 下载 foreman-definitions.json）/⬆导入（file input→POST，状态回显）+ zh/en i18n。**附带** `scripts/gen_definition_key.py` 生成密钥；`cryptography>=42` 入 client extra。三关：①**Code Review**（subagent，无 high）——加密 round-trip/明文密文共存/dup-raise 还原明文、导入幂等双查重/悬空 link 丢弃/加密 fail-closed、路由顺序、无 body 泄漏 全核对；按 **MED 修**：导入的 active 版本经 set_definition_active 重走防「同 (kind,name) 两个 live」；按 **LOW 修**：导入补 MAX_BODY 上限 + scope/metadata/status 消毒。②**安全**（subagent，无 critical/high）——确认 边界(app.py 仅 shared、server 不 import client/crypto、team serve→503)、crypto.py 懒导入依赖轻、**密钥/ body 零泄漏**(仅 gen 脚本打印密钥；事件/响应/日志/git 皆无)、Fernet decrypt 篡改/坏钥**fail-loud 不返垃圾**、导入有 1万条 + body 上限防 DoS、全程无 shell/eval、body 只存取不执行；LOW(导入 body 上限)已按上修。③**PM 验收**——对照 §765/§11.2C：导出/备份(整包 JSON+携带) + 还原(幂等 merge) + 可选 body 加密(落库密文/可逆/默认明文向后兼容) 全达成。**Playwright/chromium 浏览器验收通过**（4/4：渲染 2 条秘方→点⬇导出得 foreman-definitions.json[format+2 defs]→清库刷新空态→选文件⬆导入→2 条秘方重现 + 状态「已导入: 2」；三截图留证）。**534 tests 全绿（+31：test_crypto.py 11 + test_definition_export.py 16 + 导出导入 API 4）+ ruff 通过**。client⊥server 边界守住（crypto 在 shared、加密只在 client Store、秘方只在本地库 §8.3）。**Live 延后**：无（纯本地，密钥由用户 .env 配，加密默认关）。
- [x] **T6.3** `Notifier` 接口（飞书/Telegram/Bark/邮件）+ 插件 entry points。✅ 第二层「偶尔才用的代码级扩展」落地（§776-777，纯 `shared`，只依赖 stdlib + httpx[base dep] + shared.config，无 client/server 泄漏 §14）：①**统一接口**——`Notifier.send(title, body, buttons)` **就一个动作**（§776），返回 `sent/disabled/error` 且**绝不抛**（坏渠道不拖垮调用方，同 Pusher 硬化思路）；`NotifyButton{text,url}` 表「按钮」（深链卡片用）。②**四个内置渠道**（HTTP/SMTP client 全可注入→无网络单测）：`FeishuNotifier`（飞书自定义机器人 webhook，text 消息 + 按钮追加成链接行，code==0 判成功）/`TelegramNotifier`（Bot API sendMessage，按钮→inline_keyboard，ok 判成功）/`BarkNotifier`（首按钮→tap URL、其余进 body，code==200）/`EmailNotifier`（stdlib smtplib，按钮→正文链接行，starttls 默认验证证书）。③**插件 entry points**（§777）——`load_plugin_notifiers` 经 `foreman.notifiers` entry-point 组发现第三方 `foreman-plugin-xxx`（工厂 `(config)->Notifier|None`），**坏插件逐个 try/except 隔离、跳过不崩启动**；`NotificationHub.from_config` = 内置（直载，无需重装）+ 插件，按 name 去重（**插件不能覆盖内置**，内置赢），`notify` 同步扇出收 `{渠道:结果}`、`notify_async` 经 to_thread 不堵事件循环。④**配置/机密**——`NotifyCfg`（per-channel enabled/地址/host）在 config.yaml；**机密**（webhook/token/bark_key/smtp_password）只在 `.env`/`Secrets`（FOREMAN_ 前缀），各渠道默认 disabled。`.env.example` + pyproject entry-point 组用法注释补上。**§15 N/A**（非 LLM 文本组件，无提示词）。三关：①**Code Review**（subagent，**APPROVE-WITH-NITS**）——never-raise/按钮渲染/成功分类/插件隔离/去重 全核对；按建议修：**半填按钮**（缺 text 或 url）经 `_usable_buttons` 丢弃（防悬空 `": url"` 行 + Telegram 拒空 label）+ to_addrs strip；保留宽松成功判定（2xx+无显式失败标=成功）并记决策；②**安全**（subagent，**PASS-WITH-NOTES**，无 CRITICAL/HIGH/MEDIUM）——确认 无 shell/eval/exec/不安全反序列化、**机密零泄漏**（日志仅静态串+exc_info、不入通知正文、不进 git）、端点 operator-controlled 无 SSRF（按钮 URL 仅收件端展示不被 Foreman 抓取）、插件失败隔离不提权、§14 边界 grep 干净、HTTP timeout 10s/SMTP 15s；LOW（SMTP 默认 SSL context 在 3.11+ 已验证）接受；③**PM 验收**——对照 §776-777：一动作接口 + 四渠道实现 + entry-point 插件自动加载（启动不被坏插件拖垮）+ 核心不动 全达成。**560 tests 全绿（+26：test_notify.py 渠道/工厂/插件/hub）+ ruff 通过**。client⊥server 边界守住（纯 shared，无 server/UI 故无需部署/浏览器验收）。**Live 延后**：真投递需用户配 webhook/token/SMTP 凭据（.env）+ 各渠道 enabled；接入 Gate/Briefing 作 Web Push 并行渠道的注入点（`NotificationHub`）已留缝，归后续/团队上线。
- [x] **T6.4** 仓库内置脱敏示例定义。✅ 给开源用户一套「能跑、能照着改」的起手秘方（§765/§11.2C「仓库里只放几个脱敏示例让别人能上手；你的真东西只躺在你自己的库里」）——`foreman.db` 不进 git，没有这些示例新装就是空库。新增**数据包** `src/foreman/examples/definitions/`（纯数据，非代码）：①`manifest.yaml` 列每条的 kind/name/对应文件/scope；②8 条**通用脱敏**示例覆盖全四种 kind——workflow `add-feature`（写测试→实现→自审+lint→push 前**审批闸**，正是 DESIGN §11.2 那个例子）+ skill `write-tests`/`implementation-notes` + code_standard `python-style`/`test-naming` + qa_rubric `covers-happy-path`/`meets-standard-no-shortcuts`/`lint-passes`；每条都自标「通用脱敏示例」、**零机密**（示例正文本身还在教「别把密钥写进代码」）；技能/规范用 Markdown、工作流/QA 用 YAML（只读不执行）；README 讲怎么用。**载入器** `client/core/examples.py`（纯 client/core + shared，§14 边界）：`load_example_definitions` 经 `importlib.resources.files`（source+wheel 两种安装都解析得到）读 manifest+正文 → `ExampleDefinition`（每条打 `metadata.example=true` 与你自己的区分）；`seed_examples(store)` **幂等**写入本地库——**按 (kind,name) 是否已存在任意版本跳过**（不只看 active，故 `activate=False` 重跑也不产 v2 重复 / 不覆盖用户自有同名秘方）。**CLI** `foreman seed-examples`（懒导入保 `serve` client-free，从 config 取 db_path/definition_key 同 local_app）。pyproject 加 `package-data` 让 .yaml/.md 随轮子发布。**§15 N/A**（纯数据+文件 I/O，无 LLM 文本组件）。三关：①**Code Review**（subagent）——发现并修**真 bug**：原跳过判据只看 active 版本→`--no-activate` 重跑会重复插 v2；改为「任意版本存在即跳过」，加回归测试锁定（幂等不依赖激活 + 不覆盖用户自有名）；确认 importlib 嵌套路径解析（已建 wheel 验 8 条正文皆载入）、workflow 引用块全部随仓库附带（manifest×workflow 名字交叉核对 + 引擎 `_resolve_material` 端到端零 missing）、package-data glob 深度正确、CLI 懒导入/config 字段正确、load 失败闭合。②**安全**（subagent，**PASS**）——确认 示例正文零机密（读全部 9 文件 + 6 种密钥形 needle 测）、**正文只读不执行**（`yaml.safe_load`、无 eval/exec/subprocess/shell/pickle）、client⊥server 边界干净（examples.py 仅 import shared+store.models+definition_service、server 不 import client、秘方只入本地 Store §8.3）、无注入/DoS/不安全反序列化；按 LOW 加固**路径穿越纵深防御**：manifest `file:` 经 `_safe_rel` 拒 `..`/绝对路径/盘符/反斜杠段（即便日后 manifest 变可信外输入也逃不出 examples 目录），加 8 例参数化测试。③**PM 验收**——对照验收行+§765/ROADMAP P6：脱敏示例随仓库发布 + 开源用户 `foreman seed-examples` 一键起手 + 自洽可跑（工作流引用块全附带、引擎零 missing）+ 你的真东西仍只在本地库（foreman.db 不进 git、example 打标、不覆盖用户秘方）全达成。**非 server/非新增 UI 任务**（复用 T6.1/T6.2 既有编辑器/导入面），故无需部署/浏览器验收。**585 tests 全绿（+25：tests/test_examples.py——load/形状/唯一性/工作流自洽/seed 幂等[含 no-activate 重跑 + 不覆盖用户名]/引擎端到端零 missing/脱敏 6 形/路径穿越 8 例）+ ruff 通过**。**P6 阶段完成**。client⊥server 边界守住（纯 client/core + shared + 数据包）。

## P7 — 团队 / 总机模式
- [x] **T7.1** Relay 总机：多本地进程出站接入 + 按账号路由。✅ 把 T3.2 交付的「契约/握手/路由/心跳-mock」真正**接进运行的 `foreman serve`**（§8.5 live 接线）。**①团队模式开关**——`ServerCfg` 加 `mode`（personal[默认]/team）+ `db_path`（服务器/总机库，§7.2，**只含账号/key 哈希/进程注册表，绝无秘方/diff/LLM key** §8.3）。**②serve 装配** `build_serve_app(cfg)`（server/app.py，team 分支**懒导入** ServerStore/Relay/AuthManager 故 personal 导入面不变）：personal→`create_app(cfg)` **逐字节同旧**（部署的线上服务器不设 mode→行为完全不变、/health 不受影响）；team→建 ServerStore.init()+EventBus+`Relay(store,bus)`+`AuthManager(store)` 注入，**`store=None`**（总机盒无 client 式本地库——会话/卡走展示缓存 T7.5，故 `/api/sessions`→503；relay/auth 各自持 ServerStore）。`__main__.serve` 改调 `build_serve_app` + 模式标签。**③客户端发起的周期心跳**——`RelayConnector` 加 `heartbeat_interval`（默认 30s，<=0 关）+ 独立 `heartbeat_sleep`（与 backoff sleep 分离，注入一个不扰另一个）；`run_once` 现以 `asyncio.wait(FIRST_COMPLETED)` 并发跑 `_read_loop`+`_heartbeat_loop`，先完成者驱动断连、取消另一个并重抛其异常（run() 退避重连路径不变）。**④防 ping/pong 弹射风暴**——两端统一规则：收到任何 heartbeat 都刷 `last_heartbeat`，但**仅对 ping（裸 heartbeat）回 pong、对 pong 终止不回**（两端都初始化心跳后若都回会无限弹射）；裸 heartbeat 向后兼容仍回 pong（旧测试不破）。三关：①**Code Review**——build_serve_app personal 分支零行为变更（部署安全）、team `store=None` 使会话端点 503 不泄空数据、run_once 的 asyncio.wait done/pending 取消+gather(return_exceptions) 无悬挂任务/无未取异常警告、心跳任务用真 asyncio.sleep 默认故 backoff 测试 `sleeps` 不被污染、ping/pong 守卫双端对称且裸 heartbeat 向后兼容 全核对；②**安全**——无 shell/eval/subprocess 新增；db_path 来自可信 config.yaml（非用户输入）无注入；team 注入的 ServerStore 仅哈希/路由（§8.3 不含秘方/diff/LLM key）、account_id 握手时服务端由 key 派生（T3.2 守卫不变）；心跳帧仅 `{ping/pong:True}` 无机密；**pong 守卫实为修掉「双端初始化心跳→无限弹射」的潜在 DoS**；access_key 仍仅在 hello 帧（不变）；登录限流属 T3.5 既记延后项；client⊥server 边界 grep 干净（server 不 import client；local_app 的 client→server 注入是既有人模式既定模式）；③**PM 验收**——对照验收行+§8.5：**多本地进程出站接入**（新增**端到端集成测试**经**真实 `/relay` 端点**连 3 个进程[alice 两台 k1/k2 + bob 一台 k3]，断言各自注册 online）+ **按账号路由**（断言 `get_online_processes`/`clients_for` 按账号隔离：a1={k1,k2}、a2={k3}，a1 流量到不了 a2 机器）+ ③心跳（客户端周期 ping + 退避重连 T3.2）全达成。**591 tests 全绿（+6：read_loop pong 守卫/run_once 周期心跳/server pong 不弹射/build_serve_app personal·team/多进程按账号路由集成）+ ruff 通过**；本地 personal·team 双模 `/health` 200 烟测通过。client⊥server 边界守住。**Live 延后**（凭据/基建门槛，同 P2–P6 惯例）：①`foreman app` 真 wss 拨出（需真 access key + 已部署 team 服务器 URL，连接器+心跳已全单测，PC 端 dial-out 接线归 live rollout）；②PWA 请求经总机**端到端代理**（请求/响应跨链路相关联 + 展示缓存）归 **T7.5**（cache_sessions/cache_cards）+ 决策回路转发；本任务交付 serve 团队装配 + 客户端周期心跳 + 经真端点的多进程按账号路由接入。
- [x] **T7.2** 管理员控制台：建用户 + 邀请（无自助注册）。✅ 把 §8.2「管理员在控制台建用户、发邀请/初始密码、不开放自助注册」落地（团队模式，纯 server，复用 T3.5 的 AuthManager/ServerStore，app.py 仍只 import shared §14）。**①邀请存储**（`store/db.py`，复用既有 `invites` 表 §7.2，**只存 code 哈希** §8.4）：`add_invite`/`get_invite_by_hash`/`mark_invite_used`/`invalidate_account_invites`（重发时烧掉旧未用码 → 恒「一个活码」）。**②AuthManager 新方法**：`invite_account`（建**无密码** `invited` 态账号 + 发一次性邀请码，明文只回一次）/`reinvite_account`（重发，先烧旧码）/`redeem_invite`（**唯一的非管理员设密码路径**＝兑换邀请→设密码+激活+烧码+直接登录发 token；**fail-closed**：缺/已用/过期码一律 generic `bad_code` 不泄存在性、短密码<8 在烧码前先拒[`bad_password`]故打错不浪费邀请、账号被停用则拒）/`list_accounts`（仅元数据，**绝不含 password_hash**）/`set_account_enabled`（停/启用，停用即时锁出经 resolve_token 复检 status）；抽出 `_issue_login_token` 供 login+redeem 共用（login 行为逐字节不变）。**③REST**（app.py，team 注入 auth 才活，否则 503）：`require_admin`（链 require_account 再查 role==admin → 无 token 401／非管理员 403）→ `GET /api/admin/accounts`、`POST /api/admin/accounts`（给密码=即时 active／不给=发邀请码，409 重名）、`POST /api/admin/accounts/{id}/invite`（重发）、`POST /api/admin/accounts/{id}/status`（停/启用，**拒绝停用自己** 400 防自锁）；`POST /api/auth/redeem`（**无需鉴权**＝新用户起手路径）。**④PWA 控制台**：自包含 `admin.html`/`admin.js`（管理员登录→建用户表单[用户名/显示名/角色/可选初始密码]→邀请码只显示一次+账号列表带停/启用·重发；**全 textContent 防 XSS** 渲染用户名/显示名 + zh/en i18n）+ `redeem.html`/`redeem.js`（粘邀请码+设密码→激活，错误码→人话提示）；app.css 加表单样式。三关：①**Code Review**（subagent，**APPROVE**，无 high）——单用/过期/generic-error/admin 闸/login 重构无行为变更/invites 表无需迁移（早在 SERVER_TABLES）全核对；按 LOW 修：setStatus/createUser 错误响应 `r.ok` 守卫 + `.catch(()=>{})` 兜非 JSON 错误体 + account-list 去掉 data-i18n 避免切语言闪掉已渲染行。②**安全**（subagent，**PASS**，无 critical/high）——邀请码/密码/token **只存哈希**（SHA-256/PBKDF2-240k）明文只回一次、**唯一非管理员设密码路径=兑换邀请**（无自助注册端点）、role 服务端归一化防越权、redeem 不泄码存在性 + login 等时（dummy hash）、SQLModel 参数化无注入、admin/redeem 页 textContent 无 XSS、密码≥8 服务端强制、admin 不能停用自己、server⊥client 边界干净；LOW（管理员设初始密码不走 8 位下限＝管理员自负、邀请码 256bit 无需限流）记为可接受。③**PM 验收**——对照 §8.2：建用户 + 邀请/初始密码 + 不开放自助注册 全达成；**Playwright/chromium 浏览器验收通过**（管理员登录控制台→建 alice[invite 模式]→邀请码显示+列为 invited→/redeem.html 兑换设密码→「已激活」→alice 可登录 + 邀请码单用，2 截图留证）。**610 tests 全绿（+17：test_admin_console.py 16 + test_web_page.py 1）+ ruff 通过**。server 侧任务：personal 模式 admin 端点 503/静态页可达但 API 闸守住（不影响线上 /health），push 触发部署后验 /health 200。**Live 延后**：团队模式真上线（真 access key + 已部署 team 服务器）归 live rollout；PWA 主页消费 redeem 发的 token 归后续团队 PWA 鉴权任务（redeem 已存 `foreman.token` 留缝）。
- [x] **T7.3** access key：一机一张、可多张、哈希存、可吊销。✅ 把「用户登录 PWA → 生成 access key」（§8.2 步骤2）补成**用户自助的接入密钥管理**——后端「铸/列/吊销 + 握手校验」早在 T3.5/T7.1 落地（`AuthManager.create_access_key/list_access_keys/revoke_access_key` + `/api/keys` REST + `Relay.authenticate` 按哈希认证、拒吊销/拒过期），本任务补**缺的两块**：①**用户自助管理 UI**（缺）②**有效期旋钮**（§8.4「可设有效期」，`create_access_key` 早支持 `expires_at` 但 REST 层没暴露）。**①有效期接线**：`create_access_key` 加 `expires_in_days`（UI 旋钮）——`now` 只取一次同时用于 `expires_at` 与 `created_at`（无时钟漂移）、0/负=永久、显式 `expires_at` 仍优先；经既有 `_iso_plus_seconds` 算绝对到期（复用 token TTL 同款）；返回体加 `expires_at`。`_AccessKeyBody` 加 `expires_in_days: Field(ge=0, le=3650)`（**上界 10 年**防 `timedelta` 溢出成未处理 500 → 改判 422，按安全 LOW 修）；POST `/api/keys` 透传 + 回 `expires_at`。**端到端真生效**：Relay 握手 `row.expires_at <= now()`（ISO 词典序，两端同 `+00:00` 格式）在 `register` 前拒过期 key，故丢失/过期 key 即便没吊销也自动失效。②**用户自助页** `keys.html`/`keys.js`（自包含登录，任意账号管自己的 key，非 admin-only）：登录→铸 key（标签=机器名 + 可选有效期天数）→**明文只显示一次**（只存哈希 §8.4）→列表（标签/状态/有效期至/上次在线，活跃才有吊销键）→吊销（confirm 二次确认）；**全 textContent 防 XSS**（标签是用户输入）+ zh/en i18n + 切语言重渲。三关：①**Code Review**（subagent，**APPROVE**，无 bug）——now 单取复用、0/负/显式优先三路、返回含 expires_at、keys.js textContent 无 innerHTML、**确认 Relay 真按哈希拒过期/吊销（非装饰）**、新测覆盖 before/after 到期全核对；②**安全**（subagent，**PASS**，无 CRITICAL/HIGH）——明文只回一次/列表只元数据不泄哈希·明文/不记日志、create·list·revoke 全 `require_account` 限本账号、跨账号吊销 404（多租户）、Relay 不信客户端 account_id（按 key 派生）、XSS textContent、无新 shell/eval/subprocess；按 **LOW 修**：`expires_in_days` 上界（溢出 500→422）；③**PM 验收**——对照验收行+§8.2/§8.4：一机一张（label=机器名）+ 可多张（浏览器铸 2 张）+ 哈希存（明文一次性）+ 可吊销（吊销 我的台式机 而 笔记本 仍活跃=独立吊销 §7.2）+ 设有效期（有效期至 2026-07-21）+ §8.2 步骤2 用户自助生成 全达成。**Playwright/chromium 浏览器验收通过**（登录 alice→铸「我的台式机」带 30 天有效期[明文显示+列为活跃+有效期至 2026-07-21]→再铸「笔记本」[共 2 张]→吊销其一[已吊销，另一张仍活跃]；3 截图留证）。**616 tests 全绿（+6：到期计算+Relay 端到端拒过期/无到期默认[0/负]/REST 带 expires_at/上界 422/页面 ship&wire）+ ruff 通过**。server 侧任务（新增静态页 + API 字段）：个人模式不注入 auth 故 `/api/keys`→503、keys.html 静态可达但登录 503（不影响线上 /health）；push 触发部署后验 /health 200。**Live 延后**（同 P7 惯例）：团队模式真上线（真 access key + 已部署 team 服务器）归 live rollout；本任务交付用户自助 key 管理 UI + 有效期旋钮 + 端到端到期校验。
- [x] **T7.4** 多租户隔离（每条记录绑 `account_id`）。✅ 把 §8.4「每条记录绑 account_id，用户只看自己的；管理员看系统健康，看不到他人内容」从分散落地收口成**可证明的不变式**。底子早在 T3.1/T3.5/T7.1/T7.3 铺好（accounts/access_keys/process_registry/auth_sessions/invites 五张账号绑定表全有 `account_id` 外键 §7.2；`list/get_access_keys`·`get_processes`·`get_online_processes(account)`·`resolve_token`·`invalidate_account_invites` 读路径皆按账号收窄；`revoke_access_key` 带所有权校验、`register_process` 拒跨账号 re-home、Relay 按账号路由且 account_id 由 key 服务端派生不信客户端）；本任务补**缺的两个读面 + 一套隔离证据矩阵**：①**用户只看自己的**——`AuthManager.list_processes(account_id)`（你自己的在线/离线机器，**仅元数据**：id/name/online/last_heartbeat/access_key_id/created_at，进程注册表本就无 key 哈希/明文；store 查询按账号绑定 + 逐行 `account_id` 复核做纵深防御）+ REST `GET /api/processes`（`require_account`，account.id 由 token 服务端派生绝不信客户端参数）+ **PWA「你的设备」面板**（keys.html/keys.js：登录后列你自己的机器，在线/离线徽标，机器名**全 textContent 防 XSS** + zh/en i18n + 切语言重渲，只读）。②**管理员看系统健康、看不到他人内容**——`AuthManager.system_health()`（**纯聚合计数**：账号 total/active/disabled/invited + 在线进程数；**无任何**账号内容/机器名/用户名/哈希/token，§8.3 服务器本就无秘方/diff/原始返回可泄）+ REST `GET /api/admin/health`（`require_admin`，member→403）。三关：①**Code Review**（subagent，**APPROVE-WITH-NITS**）——list_processes 账号收窄无泄漏、system_health 计数正确且零内容泄漏（三状态全覆盖、total 独立算）、端点 401/403/503 与既有一致、JS 与 loadKeys 错误处理/重渲对齐 全核对；唯一 nit：SQL WHERE 已保证后逐行 `account_id` 复核为不可达防御性死代码——按设计保留为有文档的 belt-and-suspenders（两评审皆接受）。②**安全**（subagent，**PASS**，无 critical/high）——确认 跨租户零泄漏（作用域服务端从 token 派生 + store 参数化查询 + 逐行复核 + re-home 守卫三重）、system_health 严格聚合无内容/机密、鉴权 401/403/503 矩阵正确、新 JS 全 textContent 无 XSS、SQLModel 参数化无注入、无新 shell/机密入日志；LOW（admin 见全站计数=设计内的运维监督面、离线徽标用 bad 类纯视觉）接受。③**PM 验收**——对照验收行 + §8.4：每条账号绑定记录皆 account_id 且读路径收窄 + 用户只看自己（机器/密钥）+ 管理员看系统健康不看内容 + 跨账号操作被拒（吊销他人 key→not_found / 跨账号 re-home 拒 / Relay 按主人路由）全达成；**Playwright/chromium 浏览器验收通过**（seed alice[台式机 online/笔记本 offline]+bob[bob-secret-box]→alice 登录「你的设备」**只列自己两台**[在线/离线徽标]、**bob 的机器不出现**，截图留证）。**626 tests 全绿（+10：test_multitenant.py——access_keys/processes/auth_sessions/invites 跨账号隔离 + system_health 计数无内容 + /api/processes 仅本人 + /api/admin/health admin 闸 + 个人模式 503）+ ruff 通过**。server 侧任务（新增静态面板 + 两端点）：个人模式不注入 auth 故两端点 503、不影响线上 /health；push 触发部署后验 /health 200。client⊥server 边界守住（纯 server/auth_manager + app.py 仅 shared + server/web）。
- [x] **T7.5** 展示缓存（cache_sessions / cache_cards）供本地离线只读。✅ 把 §8.5 ③「本地离线期间，PWA 看服务器那份展示缓存（只读），本地一上线就同步最新」落地——总机（团队模式）为每个账号存一份**只读副本**（会话摘要 + 决策卡），本地进程一上线就推上来，PWA 在本地离线时仍能看。**①存储**（`server/store/db.py`，复用既有 `cache_sessions`/`cache_cards` 表 §7.2）：`upsert_cache_session`/`upsert_cache_card` 按**自然键** (account_id, session_id)/(account_id, card_id) 幂等 upsert（重推覆盖不堆重复）+ `get_cache_sessions`/`get_cache_cards` 按 updated_at 倒序读，**全部按 account_id 收窄**（§8.4 多租户）。**②服务** `server/display_cache.py` `DisplayCacheService`（纯 server + shared，§14 边界，像 Relay/AuthManager 一样注入 app.py）：`sync(account_id, sessions, cards)` 把一份快照入库——**fail-closed/从严**：非 list 的 sessions/cards 当空（防 dict/int 切片崩连接）、非 dict 条目跳过、缺 id 跳过、summary/payload 非 dict 跳过、**单条 >64KiB 丢弃不截断**、**每类每次 ≤1000 条**（防 DoS）；`list_sessions`/`list_cards(session_id 可选)` 读回并打 `cached:true` 标。**③总机接线**（`server/relay.py`）：新增 `KIND_CACHE_SYNC` 协议帧（`shared/protocol.py`）；`Relay(cache=)` 注入，`_on_frame` 收到 cache_sync 帧 → `cache.sync(client.account_id, …)`，**account_id 一律取握手认证出来的账号、绝不信帧里的 account_id/payload**（多租户铁律 §8.4）。**④只读 REST**（app.py 纯 shared）：`GET /api/cache/sessions`、`GET /api/cache/cards?session_id=`（`require_account`，account.id 由 token 服务端派生）；个人模式无 auth→401 / 团队无 cache→503。**⑤客户端快照构造**（`client/cache_sync.py`，纯 client + shared）：`session_summary`/`card_summary`/`build_cache_sync`——**只放展示安全字段**（会话 goal/status/时间；卡的折叠摘要 + `diff_stat` 那**一行** "3 files +124/−80"），**绝不放原始 diff/原始返回/秘方**（§8.3，完整内容仍只在本机、在线时即时拉）。`build_serve_app` 团队模式装配 DisplayCache 注入 Relay+app；个人模式逐字节不变。三关：①**Code Review**（8 角度并行 subagent）——发现并修**真 bug**：`sync` 原 `(sessions or [])[:MAX]` 遇非 list（dict/int）会 `TypeError` 冒泡出 `_on_frame` 被 serve 的 `except` 吞掉而**扯断该进程的总机连接**（违反「一条坏数据不毁整次同步」契约）→ 抽 `_items()` 非 list 即当空 fail-closed；按建议把 `item.get("summary") or {}` 改 `get("summary", {})` 使 falsy 非 dict（`[]`/`0`）也走 `_clip_json` 跳过（与 truthy 非 dict 一致）；效率（每行各开事务）/拷贝（_sync_sessions vs _sync_cards）判为可接受（与 db.py 既有逐 helper 各开 session 风格一致、两者有 status 字段差异）、app.py 的 `hasattr` 闸与该文件既有 gate/None 检查惯例一致。②**安全**（subagent，**CLEAN**，无 CRITICAL/HIGH/MEDIUM）——确认 跨租户零泄漏（写按 client.account_id、读按 token 派生 account.id、store 全 WHERE account_id、帧里 account_id 结构性忽略，**EVIL-OTHER-ACCOUNT 帧仍入本账号**有测）、两端点 require_account 先于 cache 503、SQLModel 参数化无注入、**缓存内容只存取从不执行**（json.loads/dumps，无 eval/exec/pickle/yaml/shell）、**机密零泄漏**（快照只展示字段；e2e 测断言 access key 明文/哈希/key_hash 均不入响应）、DoS 有 1000 条 + 64KiB 双上限 + 坏帧不崩连接、§14 边界 grep 干净；LOW（同一账号跨多次 sync 用变化 session_id 可无界增长，但自限于持有效 key 的本账号、单行 64KiB 封顶）**记为延后硬化**（正常本地进程推的是有界当前集）。③**PM 验收**——对照验收行 + §7.2/§8.3/§8.5 ③：cache_sessions/cache_cards 双表落地 + 本地离线只读（账号收窄 REST 读）+ 本地上线同步最新（cache_sync 帧入库）+ 总机只存展示摘要不存秘方/diff 全达成。**648 tests 全绿（+22：test_display_cache.py——store 幂等/账号收窄 + sync 存读/跳坏条/丢超大/非 list fail-closed/falsy 跳过/多租户 + relay 帧按认证账号入缓存/无 cache 不崩 + REST 鉴权&收窄&503&会话过滤 + 客户端快照展示安全/坏 options 容错 + e2e 经真 /relay 端点推快照→owner 读回/他人读不到/无机密泄漏）+ ruff 通过**。**server 侧任务**：个人模式 create_app 的 `cache` 参数默认 None、build_serve_app 个人分支逐字节不变（线上 personal 模式 /health 不受影响），新 `/api/cache/*` 个人模式 401（auth 未配）；push 触发部署后验 /health 200。client⊥server 边界守住（display_cache 纯 server+shared、cache_sync 纯 client+shared、app.py 仅 shared）。**Live 延后**（凭据/基建门槛，同 P7 惯例）：①连上总机后**周期推快照**的真循环（需真 access key + 已部署 team 服务器，快照构造器已全单测）；②PWA **离线兜底 UI** + **在线时端到端代理**（请求跨链路相关联）+ **决策回路转发**归 live rollout（T7.1 既记的延后项，本任务交付其中的「展示缓存」半边）。
- [x] **验收**：3 人各跑各的本地进程、共用一台服务器、互不见秘方与数据。✅ **多租户隔离在集成层已证**（互不见秘方与数据）：①**秘方/diff/原始返回根本不在服务器**（§8.3——definitions 只在本地库、总机库无该表；展示缓存 T7.5 只存折叠摘要 + diff_stat 一行，安全审计断言无原始内容/机密泄漏）；②**每条账号绑定记录皆按 account_id 收窄**（T7.4 隔离矩阵：access_keys/processes/auth_sessions/invites + T7.5 cache_sessions/cache_cards，读写双向收窄、跨账号操作被拒）；③**多进程经真 /relay 端点按账号路由**（T7.1 集成测试：alice 两台 + bob 一台，a1 流量到不了 a2）；④展示缓存 e2e（T7.5）：一进程推快照 → owner 读回、另一租户读不到。**3 人真上线**（真 access key + 已部署 team 服务器 + PWA 端到端代理/周期推送）是凭据/基建门槛的 **live rollout 延后项**（贯穿 P7 各任务记录，同惯例），隔离不变式本身已由单测/集成测试逐条锁定。**P7 阶段完成 → 整个 roadmap（P0–P7）实现完毕**。

---
*维护：完成一步即勾选；阶段级里程碑见 [ROADMAP.md](ROADMAP.md)，设计依据见 [DESIGN.zh-CN.md](DESIGN.zh-CN.md)。*
