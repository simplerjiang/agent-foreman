"""Operator Toolbelt: computer-use executors (DESIGN §4.7) — placeholder for P4.

Screenshot (hide / show / highlight-magnify cursor), mouse, keyboard. Runs in the user's
interactive session — no Session-0 limit, because the PC app is a normal session app
(DESIGN §3.1). Every use still flows Auditor → Gate → decision card (DESIGN §6.4).
"""
